# 月華社 官网 · 视觉方向定稿（M1）

> 风格方向：**B · 清冷极简**
> 参考：Ólafur Arnalds 官网、jizue 官网
> 所有数值的唯一真源是 `src/styles/tokens.css`，本文只解释取舍。

---

## 1. 从参考站提取的设计语言

看这两个站，反复出现的其实是同一套克制的手法：

| 手法 | 参考站的做法 | 我们怎么用 |
|---|---|---|
| 导航 | 纯文字，大写，宽字距，无背景无边框 | 中文导航同样做宽字距，激活项用一条 1px 下划线 |
| 首页 | 不陈列作品，只做门户 | 首页只放最新 4 张专辑 + 3 条动态，其余进二级页 |
| 留白 | 区块间距极大，首屏几乎只有一行字 | 区块上下 64–120px，正文行高 1.8 |
| 色彩 | 近黑白，强调色只在链接上出现 | 单一强调色，且只用于链接、激活态、标签底 |
| 图片 | 不抢戏，方图，无圆角或极小圆角 | 封面 1:1，圆角 4px，悬停只上浮 3px |
| 语言切换 | 两个字母，无下拉框 | `ZH / JA / EN` |

**没有采用的做法**：卡片阴影堆叠、大面积色块、装饰性图形、Hero 大图轮播。这些都会把「作品」降格为配角。

---

## 2. 配色

中性色刻意带一丝冷调蓝（不是纯灰），避免灰色显得脏；整体压得很淡，让留白成为主要材料。

| 令牌 | 值 | 用途 |
|---|---|---|
| `--paper-0` | `#FFFFFF` | 卡片、内容面 |
| `--paper-50` | `#FAFBFC` | 页面底色 |
| `--paper-100` | `#F2F4F6` | 内嵌区块、占位图 |
| `--line-1` | `#E3E7EA` | 发丝分隔线（默认边框） |
| `--line-2` | `#CFD5DA` | 较强边界、输入框 |
| `--ink-900` | `#0B0E12` | 标题、强调 |
| `--ink-700` | `#2E353D` | 正文 |
| `--ink-500` | `#6B737C` | 次要文字 |
| `--ink-400` | `#8B939C` | 日期、编号、元信息 |
| `--ink-300` | `#AAB1B8` | 占位、禁用 |

### 强调色 · 三选一（待你定）

| 方案 | 名称 | 值 | 气质 |
|---|---|---|---|
| **A**（当前启用） | 月白青 | `#3F6E8F` | 冷青蓝，与「月」的意象最贴 |
| B | 鈍銀 | `#5F6C77` | 几乎全灰，最克制，但可能显得没重点 |
| C | 霜紫 | `#6C6A93` | 带一点夜色，个性最强，也最挑封面配色 |

切换方式：改 `tokens.css` 里 `--accent` 那四行，其余全站自动跟随。样张页三种方案有并排预览。

**全站只有一个强调色**，不新增第二个。寒色系配色一旦出现两个强调色就会散。

---

## 3. 字体

### 选型：MiSans（自托管）

| 覆盖 | 字体 | 说明 |
|---|---|---|
| 拉丁、数字 | **MiSans** | 与汉字同源，混排不跳字 |
| 简体中文 | **MiSans** | 覆盖 GB18030-2022，生僻字（人名、地名、古籍用字）不会掉 |
| 日本語 | **MiSans** | 假名由 Monotype 专门制作；汉字为中文字形，见下方「日文字形」 |

全站非衬线，**一套字体覆盖三种文字** —— 这是选 MiSans 的核心理由。
此前的方案是「拉丁走 Inter、汉字回退思源黑体」，两套字体拼在一起时，
字面大小与笔画粗细有细微落差，三语站会把这个问题放大。

**授权**：小米科技免费商用授权，但有两条硬约束 ——

1. 使用方须注明使用了 MiSans 字体（页脚已署名，勿删）
2. 不得改编字体本身，不得单独分发或售卖字体文件

子集化仅用于网页传输，未改动字形。协议全文见 `scripts/fetch-fonts.mjs` 顶部注释。

### 交付方式：分片自托管

MiSans 完整字库 2.7 万字符，单个 woff2 超过 5MB，不能整包下发。
实际做法是**按 Unicode 码位切成 100 个分片**，用 `@font-face` + `unicode-range` 声明：

- 浏览器只下载「当前页面实际用到的字符」所在的分片，典型页面 5–12 片
- 三档字重 × 100 片 = 300 个文件，仓库体积约 6.5MB（一次性）
- 字体 CSS 约 256KB，gzip 后约 88KB，浏览器会缓存

生成与升级：`npm run fonts`（改版本号后加 `--force` 重新下载）。
产物在 `public/fonts/misans/`，由脚本生成，**不要手工编辑**。

**页面里没有任何第三方字体域名。** 这是刻意的 —— 静态站不该因为一个 CDN
在国内不可达，就把整个设计降级成系统字体。

字重映射：站点的 300/400/500 对应 MiSans 内部的 250/330/380。
映射写在生成脚本里，必须在 CSS 里显式声明 —— 声明值与字体文件不匹配时，
浏览器会自己合成假粗体，那比字重不准更难看。

### 字形自检

MiSans 有已知的字符缺口（日文 `・`、`〜` 等），而站点内容会长期增长。
`npm run build` 会自动扫描构建产物里**实际会显示的字符**（已剔除注释与脚本），
与 MiSans 字符集比对，缺字即报警。`npm run check:glyphs` 可让它在缺字时直接失败，用于 CI。

### 日文字形（已知局限）

MiSans 免费版**没有独立日文包** —— 官方下载列表只有 SC / TC / Latin / L3 及各民族语言文字。
因此日文页：

- **假名正常**：MiSans 的假名由 Monotype 专门制作
- **汉字按中文规范显示**：日文写法不同的字（如「直」「骨」）会有细微差异
- **日文特有条目符号会回退**：MiSans 缺 `・`(U+30FB)、`〜`(U+301C)，
  回退链已把日文字体排在前面，这些字符会落到系统日文字形上

对「社团介绍 + 专辑信息」这类内容影响有限。若将来要严格的日文字形，
可选方案是日文页额外自托管 Noto Sans JP 分片（约 +2MB 仓库），或等小米发布 MiSans JP。

### 字号阶梯

| 令牌 | 尺寸 | 字重 | 行高 | 用途 |
|---|---|---|---|---|
| `--text-display` | clamp(38→64px) | 300 | 1.12 | 首页社团名 |
| `--text-h1` | clamp(28→40px) | 300 | 1.35 | 页面主标题 |
| `--text-h2` | clamp(22→28px) | 300 | 1.35 | 区块标题 |
| `--text-h3` | 18px | 400 | 1.35 | 卡片标题 |
| `--text-body` | 15px | 400 | 1.8 | 正文 |
| `--text-sm` | 13px | 400 | 1.5 | 次要文字 |
| `--text-meta` | 12px | 500 | — | 编号、日期、标签 |

**整站只用 300 / 400 / 500 三档字重**，不用 600 以上。大标题用 300 制造轻盈感，这是"清冷"感的主要来源。

字号用 `clamp()` 做流体缩放，移动端不靠媒体查询硬切。

---

## 4. 布局与间距

- 间距基数 **4px**，令牌 `--s-1` ~ `--s-10`（4 / 8 / 12 / 16 / 24 / 32 / 48 / 64 / 96 / 128）
- 页面外框 `--w-container: 1120px`
- **长文单栏 `--w-text: 680px`** —— 中文约 40 字/行，是阅读舒适区
- 区块垂直间距 `clamp(64px, 9vw, 120px)`
- 左右留白 `clamp(20px, 5vw, 48px)`

圆角刻意做得极小（2 / 4 / 8px），接近直角 —— 圆角越大越"友好"，与清冷调性相反。

---

## 5. 组件

| 组件 | 文件 | 设计意图 |
|---|---|---|
| 顶部导航 | `SiteHeader.astro` | 粘顶 + 毛玻璃，页面滚动时导航不离开视线；激活项一条下划线 |
| 语言切换 | `LangSwitch.astro` | `ZH / JA / EN` 三个字母，当前语言墨色实心 |
| 按钮 | `Button.astro` | 三档：实心墨黑（一页最多一个）/ 发丝描边 / 纯文字带下划线 |
| 专辑卡片 | `AlbumCard.astro` | 封面 1:1；封面缺位时显示编号占位，**布局不跳动** |
| 曲目表 | `TrackList.astro` | 二创曲自动带出「原曲 / 出处」；原创曲与二创曲的署名差异被统一成同一列 |
| 动态时间线 | `Timeline.astro` | 按年月分组；左侧日期（22px / 近黑）是列表的视觉锚点，右侧标签 + 正文 |
| 文章卡 | `PostCard.astro` | 有封面时缩略图在左；无封面收成单列，不留空的图位 |
| 相邻导航 | `Pager.astro` | 上一篇 / 下一篇（专辑页是上一张 / 下一张），文章与专辑共用 |
| 成员卡 | `MemberCard.astro` | `status` 三值枚举（正式 / 合作 / 退出）决定标记与分组，见 §21.2 |
| 滚动淡入 | `Reveal.astro` | 见 §6 |
| 站内搜索 | `pages/search/index.astro` | 索引由内容集合现场推导；没有 JS 时这一页退化成一份完整的站内索引，见 §23 |
| 进场遮罩 | `Intro.astro` | 只在首页；「进站」时挡在首屏上的纸色遮罩，指示物是一条约画的弧，见 §25 |

### 一处刻意的偏差，需要你确认

需求 §2.2 写的是「封面网格，**悬停显示**标题/日期」。我改成了**标题与编号常驻显示**，悬停只做上浮与变色。

理由：悬停才出现的信息在触屏上完全不存在，而你自己判断手机是主要入口；且专辑标题是主要检索依据，不该藏起来。

如果你坚持原方案，改动只在 `AlbumCard.astro` 一个文件里。**建议保留常驻显示。**

### 关于东方 Project 二创的合规处理

曲目表把「原曲 / 出处作品」作为固定字段渲染，不是可选项。这是上海爱丽丝幻乐团二次创作指南的硬要求（标注原曲及出处），也让"这首原曲你们编过没"这类查询能成立。

---

## 6. 动效

按你选的「中等」程度，但**只保留服务于阅读的部分**：

| 场景 | 时长 | 做法 |
|---|---|---|
| 悬停变色、边框加深 | 180ms | 纯 `transition` |
| 下划线展开、封面轻微上浮 | 320ms | `transform: scaleX/translateY` |
| 滚动进入淡入 | 560ms | `opacity` + `translateY(14px)`，IntersectionObserver 触发，出现后即 `unobserve` |
| 页面切换 | 出场 180ms / 入场 320ms | 幕布淡入 + 新页淡入，都只动 `opacity`，见 §24 |
| 首页进场 | 画圈 1000ms + 停 200ms / 退场 560ms | 只在会话首次进站；弧从无画到整圈，退场时主视觉按 85ms 一档错开浮入，见 §25 |

统一缓动 `cubic-bezier(0.22, 1, 0.36, 1)` —— 起步快收尾慢，观感安静不弹跳。

**两条硬约束：**

1. 用户系统开启「减少动态效果」时，所有时长归零（`tokens.css` 里用媒体查询把 `--dur-*` 设为 0ms）
2. 没有 JS 时内容默认可见 —— 淡入是渐进增强，不是显示的必要条件。所以 `.reveal` 的隐藏规则挂在 `html.js` 下。

**明确不做**：粒子、视差、首页动效大场面。这些在"作品集"类站点上会削弱沉静感，也拖慢手机端。

> **「页面转场动画」原先也在这条名单里**（M1 的判断）。2026-09-15 按需求 §5 勾的
> 「中等（滚动触发、页面转场）」改成做，但**只做透明度** —— 整站都是整页跳转，
> 中间那道白闪是静默的粗糙感。取舍、两个兜底与验证见 §24。

---

## 7. 无障碍基线

- 语义化标签（`nav` / `main` / `article` / `time`）
- 键盘用户第一个 Tab 是「跳到主要内容」
- 所有图片有 `alt`，装饰性图形 `aria-hidden`
- 焦点态用 1px 强调色描边 + 3px 偏移，不用 `outline: none`
- 移动端菜单按钮带 `aria-expanded` / `aria-controls`

---

## 8. M1 定稿

| 项 | 结论 |
|---|---|
| 强调色 | **A · 月白青** `#3F6E8F` |
| 专辑卡片 | 常驻显示标题与日期，不用悬停显示 |
| 字体 | 全站自托管 **MiSans**，300 / 400 / 500 三档 |
| 字体交付 | 按 Unicode 码位切片，页面内无任何第三方域名 |

以上均已落到代码：配色改 `tokens.css` 一个变量即可全局调整；
字体产物由 `npm run fonts` 生成，不要手工编辑。

**M1 遗留项，均已结清**

1. ~~首页主视觉素材~~ → 社团徽记已提供，见 §10。
2. ~~日文字形是否特化~~ → 决定**不单独引入** Noto Sans JP。多语言版本整体搁置
   （见 §10.3），日文页不存在，字形问题也就无从谈起。

---

## 9. M2 · 内容架构（专辑）

M1 定的是「长什么样」，M2 定的是「内容从哪来、怎么保证不写错」。

### 9.1 内容集合是唯一真源

专辑数据是**内容集合**（Content Collection），不是散落的 TS 常量。
`src/content.config.ts` 里的 zod schema 是字段定义的唯一出处，页面只消费不定义。

| 字段 | 约束 | 为什么卡这么死 |
|---|---|---|
| `catalog` | `^GK(CD\|SL)-\d{3}$` | 编号错一位就是另一张专辑，必须机器校验 |
| `releaseDate` | `^\d{4}-\d{2}-\d{2}$` | 字符串日期可排序，且必须补零 |
| `duration` | `^\d{1,2}:\d{2}$` | 统一 `3:25` 而非 `03:25`，避免曲目表对不齐 |
| `type` | 枚举四选一 | 分类是筛选器数据源，不能自由发挥 |

**教训**：Astro 要求 frontmatter 必须在文件**第 0 字节**。我一度把说明注释写成
HTML 注释放在 `---` 之前，结果 schema 解析直接失败，报的还是「catalog: Required」
这种指向不明的错。说明注释只能写成 frontmatter **内部**的 YAML `#` 注释。

### 9.2 曲目有两种写法

这是全站最容易写错的地方 —— 原创曲和二创曲的署名**不是同一个字段**：

```yaml
# 原创曲：用 compose
- no: 1
  title: Starfall -星降る-
  compose: Khrisma

# 二创曲：用 arrange + original + source（东方二创指南的硬要求）
- no: 2
  title: Bibliophile
  original: Dolce Biblioteca      # 原曲名
  source: 蔚蓝档案                  # 出处作品
  arrange: Khrisma                 # 本社编曲者
```

曲目表把「原曲 / 出处」作为固定列渲染，**不是可选项** —— 它同时满足合规与检索两件事。

**原创曲不标 `note: 原创曲`。** 用 `compose` 还是 `arrange` 已经说明了这件事，
再挂一个标签是重复信息 —— 等真出了一张整张原创的专辑，每首都顶着「原创曲」会很蠢。
`note` 只留给 Bonus 这类确实需要点出来的曲目，单独成一列，不与「原曲」混在一个格里。

移动端**不截断**这一列：`white-space: normal; overflow-wrap: anywhere`。
二创署名是社团最该被看清的信息，宁可行高一点也不能被 `…` 吃掉。

### 9.3 封面是两步流程

```
src/assets/albums/<id>/cover.*     ← 原图，入库版本控制
        │  npm run covers（sharp）
        ▼
public/images/albums/<id>/cover-{320,640,1200}.webp
```

三档宽度写死在 `src/lib/media.ts` 的 `COVER_WIDTHS`，页面用 `coverSrcset()` 出 `srcset`。

单张上限 **200KB**（需求 §4.3）是**硬约束**，由编码器自己走两步让步达成：

1. **降质量**：q82 → 78 → 75 → 70，达标即停。绝大多数图到这里就够了，肉眼无损。
2. **降尺寸**：仍超标才缩，每档 −10%，下限 640px（每缩一档重跑一遍质量阶梯）。

**顺序不能反。** 细节密集的图降质量几乎不省体积 —— 实测 GKCD-005（背景是整片樱花树冠）
在 1000px 下 q82=389KB，一路压到 q60 **仍 290KB**；而「清晰但小一号」远好过「原尺寸但糊」。
该图最终落在 810px / q75 / 200KB，实测 460px 槽位在 DPR2 下观感正常。

**两个必须知道的推论：**

- 缩尺寸时**文件名不变**（仍是 `cover-1200.webp`）。页面正文、JSON-LD、`og:image`
  全都按文件名引用，URL 必须稳定，所以这里选了「宁可名不副实」。
  代价是 `srcset` 里的 `1200w` 只是**名义档位**，真实像素可能更小
  （源图本身就窄时也会这样，如 GKCD-006 的 1200 档实际是 1000px）。
  浏览器按名义档位挑图，在这些图上会拿到略软的一张 —— 这是**刻意的取舍**，
  换来 URL 永不失效、维护者不用改页面。脚本会把真实像素标出来（`1200w 200KB(810px)`）。
- `npm run covers` 的退出码语义是**告警不阻断、真失败才阻断**：
  为了达标做了让步 → `ℹ️` + 退出码 0；压到下限仍超标 → `✗` + 退出码 1。
  后者**仍会写出那张最小的图**，避免页面缺档 404；单图不合格不该让整批重建失败。

没有用 Astro 内置的 `image()` —— 它并不从 `astro:content` 导出（实测），
预生成 WebP 反而让构建更快、产物体积可控。

### 9.4 抓取与人工填写共存

`npm run fetch:album -- GKCD-010` 从 dizzylab 抓元数据，但 dizzylab **没有**原曲信息，
也没有购买渠道。所以抓取脚本内建**合并保护**：

- 抓取覆盖：标题、封面、曲目名与时长、标签、外链
- 人工保留：`original` / `source` / `note` / `compose` / `arrange`，
  以及专辑级 `subtitle` / `titleAlt` / `stores`

**教训**：这个保护曾经静默失效 —— 脚本在 frontmatter 前写了 HTML 注释，
导致 `^---` 正则匹配不上，`readExisting()` 返回 `null`，人工字段被当空值全量覆盖。
现在改成**解析不出 frontmatter 就抛错中止**，绝不「当作新文件」往下走。

另一处：如果人工已填 `compose`（说明这是原创曲），抓取就**不再补 `arrange`** ——
否则会出现一首歌同时署「作曲」和「编曲」两个字段的自相矛盾。

### 9.5 部署路径

所有站内链接走 `localePath()`（见 `src/config/site.ts`），不写裸 `/albums/`。
同一份构建产物要能同时在 `gekka.saiba.moe`（根）和 `saiba.moe/gekka`（子路径）跑，
一条硬编码的绝对路径就会在子路径部署下 404。

### 9.6 SEO

- 专辑详情页输出 **JSON-LD `MusicAlbum`**，`track` 数组里每首挂 `recordingOf → MusicComposition`。
  这让「你们编过德彪西的月光吗」这类查询在搜索引擎里能被结构化命中。
- `og:type` 用 `music.album`，分享卡优先用专辑封面（1200 宽），无封面回落到 `og-default.png`。
- 样式样张页 `noindex`，不进搜索结果。
- 可离线打开的页面预览由 `npm run preview:album` / `npm run styleguide` 导出到 `design/`
  （字体 CSS 内联、根绝对路径改相对），用于脱离服务器核对排版。

**字段已在 M2 收尾时确认/收敛**

| 字段 | 结论 |
|---|---|
| `event` 首发场合 | **删除**。社团不维护这项信息 |
| `edition` 版本 | **删除**。此前默认填「通常版」是假设，一并去掉 |
| `bonus` 特典 | **删除**。同上 |
| `stores` | 只接 **dizzylab** 一家 |
| `originalComposer` | **删除**。作曲者已确认过，但曲目表决定只保留原曲名，详见 §11 |

---

## 10. M2 收尾 · 品牌徽记与范围收敛

### 10.1 徽记（Logo）

社团正式徽记：单色，月相轮廓内是一只兔的剪影，左下角祥云，右侧一道点状斜线。
它是**单色**的，所以页头直接用原色即可（与 `--ink-900` 几乎同色），不需要为深色背景另做版本。

两步流程，与封面同构：

```
src/assets/brand/logo.png      ← 母版 3000×3000，四周留白极多
        │  npm run brand（sharp 裁边 + 缩放）
        ▼
public/brand/logo-mark.png     页头 28px 高 / 页脚 32px 高 / 分享卡水印
public/favicon.png             180×180，浅底圆角方 + 徽记居中
```

几个取舍：

- **母版不放 `public/`。** `public/` 会被整体部署，而 3000px 的母版没有任何页面引用它。
  与封面一致：原图在 `src/assets/`，产物在 `public/`。
- **徽记不染色。** 保留原样便于在别处复用，它本身已接近 `--ink-900`。
- **favicon 垫了一层浅底圆角方**（`#FAFBFC`）。纯黑透明的徽记放在深色浏览器标签栏上会消失。
- **页头只给高度不给宽度**（`height: 28px; width: auto`）。徽记比高略宽，写死宽高会拉变形。

**一个已知局限**：徽记细节密度高，缩到页头的 28px 时兔子的轮廓会糊成一团。
这是**位图母版的固有限制**，不是尺寸没调好。要彻底解决需要一份**简化矢量版**
（只保留月相外轮廓 + 一抹云），届时替换 `logo-mark.png` 并同步 favicon 即可。

### 10.2 专辑页做减法

用户要求精简，落实为三处：

| 改动 | 理由 |
|---|---|
| 删掉「试听与购买」区块 | 头部两颗按钮（试听走 B 站、购买走 dizzylab）就是出口，单为一家渠道铺一整个版块不划算 |
| 去掉 `edition` / `event` / `bonus` | 社团不维护这三项。留在 schema 里，等于给每张专辑增加一份长期维护义务 |
| facts 表「发行」→「发布」 | 用词统一 |

**连带影响**：`event` 从 schema 移除后，`AlbumCard` 的 `event` prop 与三处调用一并清掉；
`LINK_LABELS` / `STORE_LABELS` 失去消费者，也从 `lib/albums.ts` 删除。
删字段要顺着调用链一路清干净，否则会留下永远取不到值的死 prop。

### 10.3 多语言搁置

`/ja/` `/en/` 页面没做，但页头一直挂着 `ZH / JA / EN` —— 点了就是 404。
现在把 `<LangSwitch />` 从页头摘掉，**组件文件与 `localePath()` 全部保留**。

恢复方式：把 `<LangSwitch current={locale} />` 放回 `SiteHeader.astro` 的
`.header__tail`，再补齐 `/ja/` `/en/` 页面即可。所有站内链接本来就带语言前缀
（走 `localePath()`），不需要回头改任何页面。

---

## 11. 文案定稿与字段第二轮收敛

### 11.1 社团名与标语

| 项 | 定稿 |
|---|---|
| 英文名 | **Gekka Sound**（此前占位的 `GEKKA-SHA` 已全站替换） |
| 标语 | **于虚实交融的缥缈中，探索音乐的更多可能性。** |

英文名与标语只写在 `src/config/site.ts` 的 `SITE` 里，页头、页脚、首页主视觉、
分享卡（`design/og-card.html`）四处共用同一份 —— 改文案只改一处。

`tagline.ja` / `tagline.en` 目前是**暂译**：多语言版本搁置中，没有页面在读它们，
保留只是为了让 `Locale` 索引不缺键，等真要开外文版时再定稿。

### 11.2 曲目表不再列原曲作曲者

二创曲的署名收敛为**两件事**：原曲名 + 出处作品。

```yaml
- no: 2
  title: Bibliophile
  original: Dolce Biblioteca   # 原曲名
  source: 蔚蓝档案               # 出处作品
  arrange: Khrisma              # 本社编曲者
```

`originalComposer` 已从 schema / 类型 / 抓取脚本 / JSON-LD 里一并删除。
这是本轮唯一有代价的一项：`MusicComposition.composer` 本来对「编过德彪西吗」
这类查询略有用处，但曲目表的信息密度优先。日后若想加回来（只进 JSON-LD、
不上界面），恢复字段即可，数据本身是可再查的。

### 11.3 专辑页不再显示价格

价格从 facts 表和 schema 里都删掉了。理由是**会过期** —— 社团定价随汇率、渠道、
场贩与通贩而变，写在专辑页上等于给自己养一份要定期核对的数据。
购买入口保留（头部「购买」按钮 → dizzylab），要买的人点进去自然看到实时价格。

### 11.4 连带清理

删字段要顺着调用链清干净，这一轮的尾部：

- `TrackList.astro` 的 `originOf()` 只剩「原曲 / 出处」的拼接
- `scripts/fetch-dizzylab.mjs`：`HUMAN_TRACK_FIELDS` / `TRACK_FIELD_ORDER` 去 `originalComposer`；
  价格解析、`put('price')`、合并回写里的 `price` 一并删除
- `scripts/new-album.mjs` 的骨架模板同步
- `src/data/samples.ts` 的示例专辑去掉 `price`

---

## 12. 第三轮收敛：曲目表与 Staff

### 12.1 曲目行不再挂试听钮

每行末尾原本有一个圆形试听按钮（指向该专辑的 B 站试听）。删掉的理由：

- **出口重复。** 头部已经有一颗「试听」主按钮，行尾再放一个是同一个去处 ——
  一张 12 曲的专辑会排出 12 个指向同一链接的按钮。
- **挤窄曲名。** 那一列占掉固定的 28px 加间距，而「曲名 / 原曲·出处」正是本表
  最需要宽度的一列。

`TrackList.astro` 随之去掉了 `listenHref` prop（连 `.tracks__listen` 样式一起），
桌面网格从 6 列收到 5 列，窄屏从 4 列收到 3 列。**专辑级试听入口不受影响** ——
`[catalog].astro` 那颗主按钮仍在，走 `links.bilibili`，无 B 站时退到 dizzylab。

### 12.2 Staff 角色名归一化：封面绘画 → 绘画

站内统一叫「绘画」。除了改数据，还给抓取脚本加了 `ROLE_ALIAS` 映射：

```js
const ROLE_ALIAS = { 封面绘画: '绘画' };
```

非这么写不可 —— 否则日后新抓一张专辑，「封面绘画」又会被带回来。
有个易踩的点：`ROLE_HINTS` 里必须**留着**「封面绘画」，它是用 `^` 锚定行首的角色
识别词，删掉会让「封面绘画：XXX」整行漏识别、掉进简介正文。映射只改显示名，不改识别。

已验证：GKCD-002 / GKCD-009 / GKCD-013 三张重新试抓，角色一律输出「绘画」，Staff 无遗漏。

## 13. 正式 / 非正式：把编号里的分野显式化

`GKCD`（正式）与 `GKSL`（非正式）此前只体现在编号里，页面上毫无差别 ——
混在同一个网格里按日期排，读者无从判断哪张是社团的正式作品。

### 13.1 系列是推导出来的，不是存出来的

**没有新增 schema 字段。** 系列由编号前缀推导（`lib/albums.ts` 的 `seriesOf()`）：
schema 已经把编号限死为 `GK(CD|SL)-\d{3}`，前缀就是系列的唯一定义。
再存一个 `series` 字段，等于埋一份「可以填错、且迟早会和编号不一致」的副本 ——
而这类副本的不一致是最难发现的那种：页面照样渲染，只是标签和编号对不上。

这与 §11 的取舍同源：**能推导的就不存**。§11 删字段是减维护义务，
这里不新增字段是不制造第二真源。

### 13.2 三处呈现

| 位置 | 形式 |
|---|---|
| 卡片（`AlbumCard`） | 编号旁挂短名标记：`正式` / `非正式` |
| 详情页头部 | 同一标记，用全称 `正式专辑` / `非正式专辑` |
| 列表页 | 新增「系列」筛选组，排在年份、类型之前 |

标记抽成 `SeriesTag.astro`，只接编号、自己推导 —— 调用方不需要知道
「GKSL 对应非正式」这层映射，映射的真源只有 `seriesOf()` 一处。

筛选组排在最前，是因为它是最根本的一刀：年份与类型都是在某一系列**内部**再细分。

### 13.3 视觉上的不对称是故意的

只有**正式**那一种带强调色（`--accent` 文字 + `--accent-soft` 底），非正式保持中性灰。
两种都做成同规格的彩色标签，等于告诉读者它们地位相当，而这里要传达的恰恰是顺序感 ——
带色的那个自然读作「正」的。

同理，标记字号压在 `--text-meta`、圆角取全站最小的 `--r-sm`：
它是标注，不该跟封面和标题抢注意力。

### 13.4 网格项必须归本页所有（一次修错、一次修对）

**错的那次。** 筛选隐藏的是内层 `.grid-albums__cell`，而占网格位的是 `Reveal` 组件
渲染的外层 `div`。当时的判断是「只藏内层会留下空位」，于是写了：

```css
.grid-albums > :has(> .grid-albums__cell[hidden]) { display: none; }
```

它**静默失效**了。编译出来是：

```css
.grid-albums[cid-craod3zn] > [cid-craod3zn]:has(> .grid-albums__cell[hidden])
```

原因是 Astro 的样式作用域标记**只加在本页模板写的元素上**；组件渲染出的根元素带的是
**组件自己**的 cid（`Reveal` 是 `cid-xxxx`），所以那个 `> [cid-craod3zn]` 永远不成立。
后果不是「多一段空白」，而是更难看的一种：内层被藏了、外层仍在占位，
**筛掉的位置留下空洞，后面的卡片也不往前补位**。

**对的那次。** 把嵌套顺序倒过来 —— 网格项 = 本页渲染的 `.grid-albums__cell`，
`Reveal` 挪到它**内部**：

```astro
<div class="grid-albums__cell" data-series=… hidden?>
  <Reveal delay={i * 60}><AlbumCard … /></Reveal>
</div>
```

于是一条普通规则就够，连 `:has()` 都不需要：

```css
.grid-albums__cell[hidden] { display: none; }
```

**可复用的结论**：在 grid / flex 里**参与排布的元素必须由本页模板渲染**。
组件可以用，但要放在版位元素**里面**（或把 class 与 data 属性透传到组件根元素，
并接受样式只能走 `:global()`）。拿组件当网格项，隐藏状态就落不到版位上。
这条已同步进项目备忘，后续任何「卡片列表 + 筛选」都按这个结构写。

### 13.5 验证筛选：要断言几何，而且要证明探针会失败

第一版探针只数「可见卡片有几张」—— 数出来全对，所以这个 bug 从它眼皮底下过去了。
**数量对、位置全错**，恰是这次故障的形态。现在探针断言的是几何：
筛选后首个可见卡片的 `getBoundingClientRect()` 是否与网格原点重合
（即回到左上角），并在 1360px（4 列）与 390px（2 列）两种栅格下各跑一遍。

更关键的一步：**先证明探针会失败**。注入一段
`.grid-albums__cell[hidden]{display:block!important;visibility:hidden}` 复刻旧症状，
探针在「仅正式」下如实报出 `left=390`（第二列）→ `贴左上角=false`。
一个在正确实现和错误实现上都通过的测试等于没有测试。

筛选这类逻辑坏掉是**静默**的（页面照常渲染，只是按钮没反应），不能只靠截图判断。

---

## 14. 曲目行：给悬停衬垫留出内边距

悬停时行底是撑满整行的浅灰（`var(--paper-100)`），而序号贴最左、时长贴最右，
两层元素正好压在灰底的边缘上，看着发挤。修法是给行加左右内边距：

```css
.tracks__row {
  padding-block: var(--s-4);
  padding-inline: var(--s-4);   /* 与垂直档位一致 */
}
```

**用 `padding` 而不是 `margin-inline`。** 全局是 `box-sizing: border-box`，
padding 让内容内收而行的**外框宽度不变** —— 于是：

- 分隔线（`border-bottom`）仍与「曲目」标题的左边界对齐，块级左边界没有被破坏；
- 悬停灰底仍撑满整行，不会因为负 margin 而外溢；
- 只有行内的文字往里收，读起来像表格的单元格内边距，这是预期观感。

取值与 `padding-block` 同档（桌面 `--s-4` / 窄屏 `--s-3`），
灰底四周的呼吸感才均匀 —— 先前试过 12px 水平配 16px 垂直，灰底会显得偏「扁」。

验证：探针量「序号左边 − 行左边」与「行右边 − 时长右边」，桌面 `16 / 16`、
窄屏 `12 / 12`，且行宽 1120 未变（确认 padding 没有把行撑出容器）。

---

## 15. 专辑类型：定名、瘦身，并收敛为单一真源

类型枚举定为三个：**原创 / 东方Project / 蔚蓝档案**。

- `东方 Arrange` → **`东方Project`**。原名把「编排」这个动作写进了分类里，而这一栏回答的是
  「这张专辑取材于什么作品」，不是「我们做了什么」。何况同一张专辑里本就可能有原创曲 ——
  「原创还是二创」的职责在曲目级，已由 `compose` / `arrange` 承担，类型栏不必再背一层。
- **删掉「合作企划」**。立项时预留的类别，从未有专辑用过。一个恒为空的分支只会让筛选栏
  多一个永远点不出东西的芯片。日后真出合作专辑，按作品出处归类即可。

### 15.1 枚举真源移到 `src/lib/types.ts`

原先 `ALBUM_TYPES` 声明在 `content.config.ts`，而 `AlbumType` 联合类型在 `lib/types.ts`
又手写一遍，注释写着「加类型要同时改两处」—— 那正是迟早会失配的地方：schema 收了新值而
类型没跟（或反之），页面照常工作，只有类型检查在错的地方报错。

现在数组只声明一次，联合类型由它推导：

```ts
export const ALBUM_TYPES = ['原创', '东方Project', '蔚蓝档案'] as const;
export type AlbumType = (typeof ALBUM_TYPES)[number];
```

schema 侧改为 `import { ALBUM_TYPES } from './lib/types'`。

**没有让 `lib/albums.ts` 反过来从 `content.config.ts` 导入**，这是刻意的：
`content.config.ts` 是 Astro content layer 的配置模块，执行时会调 `defineCollection()`
这类带全局注册副作用的函数。把它拉进页面渲染的依赖图，等于让同一份集合定义在构建期被
求值两次，有重复注册的风险。类型枚举是**共享数据**而不是配置，所以下沉到不依赖 Astro 的
纯模块，两边都从那里取。

### 15.2 筛选芯片顺序改为「按枚举声明」，不再「按最新专辑」

`typesOf()` 原先返回 `[...new Set(list.map(e => e.data.type))]` —— 顺序取决于该类型首次
出现的位置，而列表是**新→旧**排的，于是每加一张专辑，芯片顺序就可能重排。读者刚记住的
位置下次就变了。

现在按 `ALBUM_TYPES` 的声明顺序过滤，顺序稳定且可控：

```ts
const present = new Set(list.map((e) => e.data.type));
return ALBUM_TYPES.filter((t) => present.has(t));
```

年份芯片仍按倒序 —— 那是刻意的，年份天然有时间方向。

### 15.3 验证

- 产物核对：类型芯片为 `全部 / 原创 / 东方Project / 蔚蓝档案`；三张专辑详情页的 facts 表
  分别显示 `东方Project` / `蔚蓝档案` / `原创`；全站已无 `东方 Arrange` / `合作企划` 残留。
- 抓取侧：`npm run fetch:album -- GKCD-006 --dry` 类型推断输出 `东方Project`，
  确认日后重抓不会把旧值写回来（这与 Staff「封面绘画」那次的处理同理）。
- 筛选交互仍用探针实测而非截图：桌面 4 列与窄屏 2 列下，三个类型各筛出 1 张，
  首张卡片回到最左列，复位回到 3 张。

---

## 16. 曲目表：溢出文字悬停自动滚动

桌面端曲目表的四列——曲名（`.tracks__title`）、原曲/出处（`.tracks__origin`）、
编曲/作曲（`.tracks__credit`）、演唱（`.tracks__vocal`）——原本用
`text-overflow: ellipsis` 截断，文字比格子长时只露出省略号，全文看不到。

改成：文字溢出时，**默认用右侧渐隐遮罩**提示「还有更多」，**鼠标悬停时自动横向滚动**
展示全文。手机端（≤720px）这四列本来就是换行不截断，不受影响。

### 16.1 为什么是「内层包裹 + transform」而不是别的

试过的方案里有两个行不通，记录在此以免后人再踩：

- **`text-indent` 动画**：想让 `text-indent` 负值把 nowrap 单行文字整体左移（这样不用改
  DOM 结构）。实测**失效**——`getComputedStyle().textIndent` 恒为 `0px`，即使直接设
  `text-indent: -100px` 也读不到。`text-indent` 作用在 block 容器的首行，但在
  `overflow: hidden` + nowrap 的溢出场景下表现不可靠。
- **外层 `transform`**：给承载文字的格子本身加 `translateX`，移动的是**格子盒子**而不是
  **文字**——文字相对盒子仍溢出、仍被盒子自身的 `overflow: hidden` 裁掉，露不出更多。

正确做法是**给文字包一层内层 `inline-block`**（`.tracks__scroll`），宽度等于完整文字，
外层负责裁切，动画作用在内层的 `transform` 上。transform 是合成器动画，不触发重排。

### 16.2 省略号换成渐隐遮罩

内层是 `inline-block`，`text-overflow: ellipsis` 对 inline-block 内容**不生效**（省略号
只作用于 inline 文本流）。所以提示「还有更多」改用右侧渐隐遮罩：

```css
mask-image: linear-gradient(to right, #000 0, #000 calc(100% - 22px), transparent 100%);
```

遮罩只加在 `[data-overflow]` 元素上（不溢出的保持干净），悬停时移除遮罩让滚动文字完整
可见。渐隐也比省略号更贴合站点的清冷极简。

### 16.3 机制与降级

- 一小段 inline JS 检测 `scrollWidth > clientWidth`，溢出才写 `data-overflow` 与
  `--scroll-dist`（溢出量）/ `--scroll-dur`（滚动时长）。**不溢出就不动**，避免满屏
  文字乱滚干扰阅读。
- 滚动时长按溢出量折算（约 30px/s，下限 3s、上限 12s），`alternate` 往返——滚到头再滚
  回来，头尾都能看清，也省去复制文本做无缝循环。
- 触发时机是**悬停**，不是常驻 marquee：表格多列若同时常驻滚动会非常吵，且分散注意力。
- `prefers-reduced-motion: reduce` 时不滚动，遮罩仍在（有「更多」提示），并给元素加了
  `title` 属性（原生 tooltip 兜底看全文）。
- 字体加载完成后重测一次溢出量（`document.fonts.ready`），避免 fallback 字体下的误判。
- 窄屏（≤720px）内层 `.tracks__scroll` 退化为 `display: inline` + 正常换行，遮罩移除，
  沿用「换行不截断」的既有行为。

### 16.4 验证

探针在 780px 桌面宽度（列较窄、逼出溢出）下断言：5 个长文字列被标记溢出、
`--scroll-dist` 全部有效、内层存在、遮罩渐隐生效、动画名正确；再用 Web Animations API
把动画精确停在 50% 进度，读 `transform` 位移 `-89px`（溢出量 179px 的一半），确认文字
确实在滚。移动端 390px 下内层 `display=inline`，降级正确。

---

## 17. 曲目库 `/tracks`

全社团 127 首曲子的跨专辑索引（REQUIREMENTS §2.3）。

### 17.1 与专辑页曲目表的分工

两张表看着像，回答的问题不同：

| | 专辑页曲目表 | 曲目库索引表 |
|---|---|---|
| 回答 | 「这一张里有什么」 | 「这首原曲我们编过几次」「谁编过这首」「这首歌收在哪张盘里」 |
| 顺序 | 专辑内曲序 | 专辑新→旧，专辑内曲序 |
| 独有列 | 时长 | 收录专辑（专辑名的链接） |

派生逻辑在 `src/lib/tracks.ts`：把 `getAlbums()` 的结果摊平成 `IndexedTrack[]`，
筛选要用的量（出处作品、编曲者、检索串）一并算好。**不新增任何内容字段** ——
数据源仍是内容集合，这与 §13.1「能推导的就不存」是同一条原则。

### 17.2 与需求草案不同的取舍

1. **原曲与出处合成一列**（同一个格子里两行，与专辑页写法一致），而不是各占一列。
   草案列了七列（曲名/原曲/出处/编曲/演唱/收录/试听），排进 1120px 后
   最该宽的曲名只剩不到 300px。
2. **索引表不挂滚动淡入。** 127 行逐行 IntersectionObserver 淡入只会让人等内容，
   密集索引要的是立刻可读。
3. **行首编号是当前列表的顺序号（1..N），不是专辑内的曲序**，并且随筛选重排。
   理由与实现见 §17.9。

**已定夺（2026-09-13）—— 草案的两列都删了，列数 7 → 4：**

- **`演唱`**：127 首里只有 2 首有值，一整列空着不值得占 72px。
  演唱信息本身没丢 —— 它仍在专辑页曲目表里，那里才是它的位置。
  （原先考虑过的「把 `vo. X` 当标签挂在曲名后」方案，随着整列删掉一并作废。）
  但它**留在检索串里**（`lib/tracks.ts` 的 `search`）：搜演唱者名字照样命中 ——
  这是删列时特意保留的行为，别当成漏删而顺手清掉。
- **`试听`**：删它的理由与 §12.1 删专辑页行尾试听钮**完全相同**，只是换到索引表上 ——
  ① **出口重复**：那一格指向的本来就是**专辑级**入口，而专辑页头部就有那颗
  「试听」主按钮；现在「收录专辑」一列又直接通向同一张专辑，一行为同一个去处
  再放 127 个重复的圆钮只是噪声。② **挤窄曲名**：那一列固定占 40px 加间距。
  `IndexedTrack.listen` 随之删掉 —— 它唯一的消费者就是这一列
  （专辑页那颗按钮走 `[catalog].astro` 自己的 `listenHref`，不受影响）。

### 17.3 一行里两个去专辑的出口，粒度不同

**曲名** → `专辑页#tracks`（直达那张盘的曲目区）；
**收录专辑那一格的专辑名** → 专辑页首页（不带锚点）。

这不违反 §12.1「同一个去处不在一行里出现两次」—— 两者的落点粒度不同：
顺着曲名点进去是想看**这首**，顺着专辑名点进去是想看**这张盘**。
这张表按专辑分组排序，一路扫下来「这张盘我还想看眼封面/Staff」是常见动作，
那时点专辑名比回头点曲名顺手。两处各只挂一个链接，不在一格里叠第二层，
专辑名与曲名同一套处理：默认无下划线，悬停才浮出强调色
（`GKCD-013` 编号仍为纯文字 —— 链接只挂在名称上）。

### 17.4 筛选：四项一律下拉

| 维度 | 控件 | 取值数 |
|---|---|---|
| 搜索 | 输入框（曲名 / 原曲 / 编曲 / 专辑） | — |
| 系列 | 下拉 | 2 |
| 类型 | 下拉 | 3 |
| 出处作品 | 下拉 | 21 |
| 编曲 / 作曲 | 下拉 | 26 |

**2026-09-13 改**：系列与类型原本是芯片，现在也换成下拉，四项控件形态统一。理由：

① 筛选条读起来是一排并列的「条件」，不再是「两个芯片组 + 两个下拉」的混排；
② 窄屏下芯片会折成好几排，下拉各占一格更整齐；
③ 少一套交互 —— 芯片的选中态、`aria-pressed`、同组互斥那段脚本全部删掉，
四个筛选共用一条 `change` 通道，重置只需把四个 `select` 归 `all`。

代价是系列/类型这两个低基数维度多一次点击（开菜单再选）。这一页的筛选以
「逐项排除」为主、不是高频切换，所以值。芯片样式仍留在 `global.css`
（专辑列表页在用）。

下拉选项**按出现次数降序**（Khrisma 90 首排最前），同次数按名称 ——
顺序不随新专辑跳动，与 §15.2 处理类型芯片的理由相同。

筛选全在客户端做（页面里本来就有全部行）：没有 JS 时最差只是「筛选不工作，内容全在」。
结果计数带 `aria-live="polite"`，筛选后读屏能听到「显示 25 / 127 曲」。

### 17.5 列头粘顶

筛选之后仍然很长，列头 `position: sticky; top: 72px`（站点导航高 72px）——
滚到中段还知道每一列是什么。窄屏（≤900px）列头隐藏、每行摊成卡片，粘顶自然失效。

### 17.6 出处作品的全角波浪号

存量数据里 `～`(U+FF5E) 与 `~`(U+007E) 混用（3 处），同一首作品会在下拉里裂成两项 ——
页面照常渲染，只是筛不全，属于最难发现的一类静默故障。两件事都做了：

- 存量一次性修齐（`gkcd-007` / `gksl-006` / `gksl-007`）；
- `lib/tracks.ts` 的 `normalizeWork()` 在读取时再归一化一次，挡住日后手工补录又写歪。

一曲多原曲写在同一字段里（`東方永夜抄　~ …; 東方風神録　~ …`），
筛选按 `;` 拆开 —— 每首作品都能单独筛到。

### 17.7 「溢出文字悬停滚动」抽成全站共用

曲目库要的正是 §16 那套机制（长文字默认渐隐、悬停横向滚动），而原来它只活在
`TrackList.astro` 的 scoped 样式里。这次抽出来：

- `src/styles/clip.css` —— `.clip` / `.clip-inner` 与遮罩、动画、`prefers-reduced-motion` 降级；
- `BaseLayout.astro` 里一段脚本 —— 测量溢出量、写 `data-overflow` 与 `--scroll-dist/dur`；
  一页只跑一次（曲目库与专辑页都不必各挂一份）。
- 组件与页面只写结构：`<span class="… clip"><span class="clip-inner">…</span></span>`。
  窄屏要改成换行不截断时，在各自的地盘上用 scoped 规则覆盖即可
  （scoped 规则编译后带 `[cid]`，特指度天然高于 `.clip`，不需要 `!important`）。

同理 `.filters` / `.chip` / `.empty` 从专辑列表页上移到 `global.css` ——
曲目库用的是同一套。**改动后专辑页与新页都重新验证过**（见 §17.8）。

### 17.8 验证

以无头 Chrome 注入探针实测，断言的是几何而不是数量（§13.5 的教训）：

- 筛选后首个可见行的 `getBoundingClientRect()` 是否贴回列表原点（`贴原点=true`）；
- 相邻可见行之间**不留缝**（`最大行缝=0.0`）—— 这条专门抓「行数对、位置全错」；
- 行数逐项对齐外部算出的期望值：类型 东方Project 74 / 蔚蓝档案 50、
  出处 蔚蓝档案 39、编曲 Khrisma 90、搜索 `dolce` 7、无结果时计数器为 0 且空态出现；
- **列模型对账**（2026-09-13 随「删试听列 + 收录专辑挂链接」加入）：列头与行都必须是
  4 格，且**列头 4 格的左边界与行 4 格的左边界逐一相同**（`125,175,865,1013`）——
  只数「每行几个格子」抓不到「删了列但栅格没跟着改」。链接逐行对账：每行**恰好 2 个**
  `<a>`（曲名 + 专辑名），曲名 → `/albums/<编号>/#tracks`、专辑名 → `/albums/<编号>/`，
  且专辑名的 `href` 必须正好是曲名的 `href` 去掉 `#tracks`。
- **变异验证**：注入 `.index__row[hidden]{display:grid!important;visibility:hidden}`
  复刻「隐藏了但仍在占位」，探针如实报出 `shown=127`（应 74）→ 证明它真的会失败。
  与正确实现和错误实现都通过的测试等于没有测试。链接那条同样做了变异：
  临时把某一行的专辑名 `href` 改成 `/wrong/`，`badRows()` 必须报出 1（实测 1），
  复位后回 0 —— 确认这条断言不是空的。
- **重复计数对账**（2026-09-13 随「（含 N 首重复乐曲）」加入）：除了读渲染出来的文案，
  再用**另一条路径**独立算一遍 —— 只取每行渲染出来的四样东西（曲名 / 编曲作曲 /
  原曲出处 / `data-type`）分组，数出「多出来的条目」；两路必须同时给出 7 组 / 8 条。
  文案侧另有三条：**两处括号逐字相同**（页头摘要 vs 表格上方计数 ——
  同一句话出现在两个地方，最易各写各的）、**静态文案与 `data-dup-note` 同源**、
  以及**筛选后由脚本重写的文案仍跟着属性**（这条专门抓「客户端把 8 写死」）。
  两份变异体各自触发不同的 FAIL（§17.10），确认这几条断言都不是空的。

  顺带记下这轮修掉的两处**探针自身**的错（都不是页面问题，是「检查」在骗人）：
  ① 断言「括号紧跟曲数」的正则我写成了 `）·`，实际文案是 `） · `（`summaryOf` 拼的是
  `${dupNote} · ${albums}`）——**探针的正则必须从真实文案里抄，不能凭记忆写**；
  ② 变异体的正则没锚定元素（详见坑 5），改了 meta 那处，页面纹丝不动。
- 1360px 与窄屏各跑一遍；窄屏下另断言无横向溢出、列头收起（`display:none`
  —— 此时列头矩形全是 0，**列边界对齐那条在窄屏无意义，只看桌面轮**）、
  行栅格 2 列、`收录` 的 `::before` 标签到位、`.clip` 溢出与残留标记都为 0（退化换行）。
  （2026-09-13 复测记录：无头 Chrome 在 Windows 上会把 `--window-size` 钳到最小 512px，
  所以那一轮窄屏实测宽度是 512 —— 仍在 900px 断点之下，媒体查询判定与 390 同一分支。
  另：`vo.` 标签随 `演唱` 列一起删了，不再断言。）
- 专辑页回归：曲目表 32 个可裁切格子中 3 个正确标记溢出，
  用 WAAPI 把动画停在 50% 读位移 `-44.0px`（溢出量 88px 的一半），确认文字真的在滚；
  390px 下退化为换行、无残留标记。

**探针自身的坑（都会让「检查」静默失效 —— 比页面 bug 更难发现。条数会继续长，所以不编号在标题里）：**

1. **注入点**：Astro 会把页面里 `is:inline` 的脚本放到 `</html>` **之后**。
   照惯例注入到 `</body>` 之前，反而排在页面脚本**之前** —— 筛选回调那时还没挂上，
   照着 DOM 派发 `change` 什么都不会发生。**探针要追加到文件最末尾。**
2. **产出不能押在定时器上**：无头 + `--virtual-time-budget` 下，写在
   `document.fonts.ready` 或 `setTimeout` 里的产出可能整段不发生，转储里只剩一段
   没人执行的脚本文本 —— 看起来正好像「页面没问题」。**结果节点先建好，同步跑完立刻写入。**
3. **只跑一轮，不要「补跑一轮覆盖」**（2026-09-13 踩到，原先第 2 条正是在教这条错做法）：
   探针一旦**与页面交互过**（点筛选、改属性、点复位），复跑读到的就是被上一轮改过的现场。
   实测代价：末尾挂了个 `setTimeout(run, 400)` 当兜底，于是每份页面都跑两轮，
   第二轮读到的是第一轮筛选/复位后**由脚本重写过的计数文案** ——
   「属性与文案一起改坏」与「只改属性」这两份本可区分的变异体，输出**收敛成同一份**，
   看着像「两份结果一致、很稳」，其实第二轮的现场已经把第一轮的证据冲掉了。
   **单轮才可复现。**（首轮真要没跑到，转储里会完全没有 `#PROBE`，一眼能看出来，
   不需要兜底。）
4. **Chrome CSSOM 两处反直觉**：
   ① 支持 CSS 嵌套后 `CSSStyleRule` 上也挂了 `cssRules`（空列表）——遍历样式表时若写成
   「有 `cssRules` 就下钻」，每条样式规则都会提前 `return`，选择器判断永远执行不到，
   探针会安静地什么都查不到。**先判 `selectorText` 再递归。**
   ② 声明里带 `var()` 时 Chrome **不把它序列化出来**：
   `.clip[data-overflow]:hover>.clip-inner{animation:clip-scroll-x var(--scroll-dur,6s)…}`
   的 `rule.style.animation` 与 `cssText` 都是空串。照 CSSOM 判会误报「规则不存在」。
   规则存在与否只能看 `selectorText`，效果则靠「强制挂载 + 读动画实测」证明。
5. **变异体的正则要锚定到 DOM 里那个元素**（2026-09-13 踩到）：
   待测的那句话常常在产物里出现多次 —— `127 曲（含 8 首重复乐曲） · 23 张专辑`
   既在页头摘要里、也在 `<meta name="description">` 里。`replace(/…/)` 不带 `g`
   只替换**第一处**，于是变异体改的是 meta、DOM 纹丝不动，输出与真实体**逐字一致**，
   看着像「页面很稳」，实际什么都没验证。
   写法：把容器一起写进正则 —— `(<p class="section-head__note"[^>]*>)…` ——
   并在注入后 grep 复核「改动确实落在预期元素上」。
   （同一条道理也适用于断言本身：先确认它**会**失败，再相信它通过。）

### 17.9 行首编号：当前列表的顺序号，不是曲序

**2026-09-13 改。** 这一列原先显示**专辑内的曲序**（`track.no`）——同一张盘的曲目
连号，换一张又从 01 开始。但第一列在视觉上就是「这份清单的第几条」，读者会当它是
全表序号。现在改成**当前列表的顺序号（1..N）**：

- **首屏**由服务端按 `rows` 的顺序直接渲染 `01..127`。与筛选后同一套规则，
  所以没有 JS 时也是对的（不是「先渲染一个占位再靠脚本改」）。
- **筛选后**由页尾脚本重排：`shown` 计数每接受一行就 `+1`，把结果写回该行的编号格。
  筛出 25 首就是 `01..25` 的连续清单。编号格在初始化时一次性缓存成数组
  （`noCells`），不在 `forEach` 里反复 `querySelector`。
- 仍用 `.mono-num`（`tabular-nums`），`01 → 100` 位数变化时列宽不跳。

「编号随筛选变动」是刻意的：这一列回答的是「在第几条」，不是「这是这张盘的第几首」。
专辑内的曲序只在专辑页那张表里有意义 —— 那里才是它的位置。
被筛掉的行走 `if (!ok) return` 直接跳过，编号不会在隐藏行上留下空洞。

### 17.10 重复乐曲：计数里的那个括号

同一首曲子会被多张盘收录（试作盘 GKSL 先收、正式盘 GKCD 再收一次，是最常见的来源），
所以「127 曲」不等于 127 首互不相同的曲子。计数后面因此挂一个
**「（含 8 首重复乐曲）」**，口径只写在 `lib/tracks.ts` 的 `duplicateCountOf()` 一处。

**判定：曲名 / 作者 / 所用原曲 / 所属分类 四项全等。**

- **曲名全等比对，绝不抹掉括号后缀。** 这是最要紧的一条 —— 一放宽就会把 Remix
  并进原版（`Mriya (2023 Rework)` 与 `Mriya (DJ Tranceair Remix)` 是两首不同的曲子，
  后者连编曲者都不是同一个人）。现有数据里 8 首 Remix 全靠「标题自带版本后缀」
  天然区分开，没有再额外做版本识别 —— **别为了显得聪明去加后缀剥离**。
- **作者取 `arrange ?? compose` 的人名**，不看署名是编曲还是作曲：同一个人把同一首
  以两种名义署两次仍是同一首；换了人就是换了一首。现有数据里把这一项从键里拿掉
  组数不变（还是 7 组），但它挡的正是「同一首原曲、他人 Remix 且同名」那种情况 ——
  留着的成本是零，去掉的代价要等真出这种事才知道。
- **所属分类 = 专辑 `type`**（原创 / 东方Project / 蔚蓝档案），不是系列。
  现有 7 组**全部跨系列**（GKSL → GKCD），把系列放进键里等于把它们全部拆散 ——
  那就成了「统计了一个不存在的问题」。
- 只裁首尾空白。人工补录多打一个空格会让重复静默漏判；这一条纯防漏，
  不会吃掉任何版本后缀。

**口径：数「多出来的条目」，不是数曲子。** 一首出现在 2 张盘上算 1 条，3 张盘上算 2 条，
于是 `总数 − 重复数 = 实际不同的曲目数`（127 − 8 = 119）能直接算出来 ——
括号里那个数是**可用的**，而不只是个提醒。另外两种口径都试过、都没选：
把重复组里的条目全部计入得 15（比「不重复曲目数」多算一份），按重复组数计得 7
（127 − 7 = 120，减不出 119）。

**展示位置：两处都带**（2026-09-13 改）。括号不只在表格上方那颗计数里，
页头 `<h1>` 下面那行摘要也带 —— 那是页面第一眼看到的数字，
只让最显眼的那处说「127 曲」而把限定条件留在下方，等于说半句话。

| 位置 | 形态 |
|---|---|
| 页头摘要（`.section-head__note`） | `127 曲（含 8 首重复乐曲） · 23 张专辑` |
| 表格上方计数（`#track-count`） | 未筛选 `共 127 曲（含 8 首重复乐曲）`<br>筛选后 `显示 39 / 127 曲（含 8 首重复乐曲）` |

括号**紧跟在曲数后面**、不放到整串末尾：重复收录是曲目的属性，
挂在「23 张专辑」后面会被读成在修饰专辑。筛选后那串里的 127 仍是同一个总数，
括号描述的是整个曲目库，**不随筛选变**。

**数据流：口径与文案各自只有一个真源。**

- 口径 —— `lib/tracks.ts` 的 `duplicateCountOf()`；
- 文案 —— 同文件的 `dupNoteOf()`，返回 `''` 或 `（含 N 首重复乐曲）`，
  由 `summaryOf(rows, dupNote)` 拼进摘要。

服务端两处（摘要 / 计数）共用同一个 `dupNote` 变量。客户端脚本**不自己拼串**
（`is:inline` 读不到 TS 模块），而是读元素上的 `data-dup-note` 属性直接贴 ——
否则「（含 N 首重复乐曲）」这句会散落成三处各写一遍，改文案必然漏掉一两处。

元素上因此有两个属性，分工不同，别混用：

- `data-dup` —— 数字本身（`8`），供校验与探针比对，**不作为文案来源**；
- `data-dup-note` —— 整串文案，客户端直接贴。

`title` 里补一句解释，省得读者猜「8」是怎么来的。
（`<meta name="description">` 引用 `summary`，于是也自动带上括号 ——
同一句话因此在构建产物里出现**两次**，这点在验证时会咬人，见 §17.8。）

**没做的：没有给重复行加标记。** 需求只要计数。真要标，得在「收录专辑」那格补一个
「另见 GKCD-008」之类的互指 —— 那是另一件事，先不做。

---

## 18. M3 · 动态 `/news`

需求 §2.5。**动态 = 短讯（一两句），杂谈 = 长文** —— 这条边界决定了形态：
动态没有详情页，一条就是一屏读完的一段；写长了那就该去杂谈。

### 18.1 一条动态 = 一个文件

内容集合 `news`（`src/content/news/*.md`），字段只有四个：

| 字段 | 约束 | 说明 |
|---|---|---|
| `date` | `^\d{4}-\d{2}-\d{2}$` | 排序与「年.月」分组都用它 |
| `tag` | 枚举 发布 / 参展 / 招募 / 公告 | 真源 `lib/types.ts` 的 `NEWS_TAGS`，筛选顺序 = 声明顺序 |
| `image` | 可选 | `public/` 下的路径 |
| `draft` | 默认 `false` | 提前写好、还没到日子发的，只在开发环境可见 |

**正文就是文件的 Markdown 正文**，没有单独的 body 字段 —— 于是「一句话短讯」和
「几段公告」是同一套写法，页面用 `render()` 渲染。加一条动态 =
`npm run new:news`（或手写一个文件）+ 写两三行正文，不碰 HTML/CSS。

与专辑同一条原则：**能推导的就不存**。标签用枚举而不是自由字符串，
是为了让筛选芯片有稳定的取值与顺序（同 §15.1 的 `ALBUM_TYPES`：加一个标签只改
`lib/types.ts` 一处）；日期仍写在 frontmatter 而不是从文件名推 ——
文件名（`YYYY-MM-DD-slug.md`）只是让目录按时间排的约定，
日期得能被 schema 校验、报错得指得出是哪一项。

`npm run new:news` 的合法标签是从 `lib/types.ts` 的 `NEWS_TAGS` 里读出来的，
脚本里不再抄一份 —— 抄了迟早会和枚举对不上。

### 18.2 正文里的站内链接写相对路径

正文是 Markdown，链接由作者自己写。**站内链接必须写相对路径**
（`[去听这张作品](../albums/GKCD-013/)`），别写 `/albums/GKCD-013/` ——
绝对路径在 D1 备用方案（`saiba.moe/gekka` 子路径）下会整条指到域名根上，
而相对路径两种部署都对、且不需要任何构建期改写。

试过做「正文里出现 GKCD-013 就自动变链接」的推导：这版 Astro 的 `render()`
没有按次传插件的口子（`remarkPlugins` 只有 `astro.config` 那一处），
只能改成全局插件 —— 为这点锦上添花让全站 Markdown 都过一遍插件不划算，
先不做，记在这里以备日后。

### 18.3 筛选：标签芯片 + 整组收起

标签少（≤4）且短，用芯片（`.chip`，与专辑列表页同一套），不用下拉。
只有出现两个以上标签时才显示筛选条 —— 一个选项的筛选点了没反应，会被当成坏了。

客户端只做一件事：按 `data-tag` 藏 `li`，再把**一条都不剩的月份组整组收掉**。
后者不是锦上添花 —— 只藏条目的话，页面上会留下一串空的「2026.09」标题。
两条 `[hidden]` 规则写在 `Timeline.astro` **自己的**样式里，因为
`.tl__item` 是 grid（作者样式会盖过浏览器默认的 `[hidden]{display:none}`），
而组件渲染的元素页面样式匹配不到（同 §13.4）。

### 18.4 正文排版在全局，不在组件里

`<Content />` 渲染出的 HTML 不带调用方的作用域标记，组件里的 scoped 样式选不中它。
所以正文排版（段落间距、链接下划线、列表记号、行内代码、引用）统一写在
`global.css` 的 `.prose` 里，页面只负责套一个 `.prose` 容器 ——
专辑简介（`.album__desc`）用的是同一份。段落间距取 `--s-5`（24px），
与专辑简介原有的一致，避免通用规则悄悄改了老页面的松紧。

---

## 19. M3 · 杂谈 `/blog`

需求 §2.4 把杂谈与动态的边界写死了：**动态 = 短讯（一两句），杂谈 = 长文**。
所以这一节基本都是围绕「长文该怎么被读到」来做：列表外显摘要、点进去看正文，
两处共用同一份数据与同一套正文排版。

### 19.1 一篇文章 = 一个文件，文件名就是 URL

`src/content/posts/*.md`，字段只有 `title` / `date` / `summary?` / `tags` / `cover?` / `draft`。
正文就是文件本身（同动态），没有 body 字段。

**与动态有一处刻意的不同：文件名不带日期前缀。**
动态的文件名是 `2026-08-15-xxx.md`，但那只是内部约定（让目录按时间排 —— 动态没有详情页）；
杂谈的文件名会**原样出现在 URL 里**，而日期进 URL 只会在改期时让链接失效。
所以杂谈的约定是 `making-of-gkcd-003.md` → `/blog/making-of-gkcd-003/`。
代价是目录不再按时间排，无所谓 —— 日期在 frontmatter 里，页面按它倒序。

标签取值 `POST_TAGS`（创作手记 / 编曲思路 / 展会Repo / 器材与软件 / 杂感）与
`NEWS_TAGS`、`ALBUM_TYPES` 同一条原则：**枚举是唯一真源**，schema 直接引用它，
`npm run new:post` 的合法值也从源码里读。但杂谈标签**只作展示** ——
不设分类页、也不做筛选：文章还少，给五个标签铺一套筛选反而会让人以为站上内容很多。

### 19.2 列表：摘要要「外显」，但图位不能靠占位框撑

卡片是「日期 / 标题 / 摘要 / 标签」，摘要就是这里唯一能外显的正文。
`summary` 手写优先，没写才回落到正文第一段（`lib/text.ts` 的 `excerptOf`）——
这个工具原先住在 `lib/news.ts` 里，接入杂谈时搬了家：摘要口径只留一份，
免得动态与杂谈的规则悄悄分叉。

**没有封面时不出缩略图列**（渲染 `post--plain`，服务端判定，不用 `:has()`）。
原先无封面会渲染一个「封面待补」占位框：一屏看下来是几个同样大小的灰框，
比没有图更难看。配图本就是可选的，版式不该为它留空位 —— 现在没有素材也能好看地列表。

### 19.3 上一篇 / 下一篇：抽成 `Pager.astro`

专辑页早有一段「上一张 / 下一张」，结构与样式跟杂谈要的一模一样，
于是抽成 `Pager.astro` 两边共用（文案与第二行小字由 props 给：文章放日期，专辑放编号）。

抽组件时**断点照抄专辑页原来的 640px**，没有顺手改成「更合理」的 700px ——
抽组件不该改变老页面的行为。验证时也把专辑页一起跑了一遍：
1360 两列 / 右槽靠右、512 单列 / 靠左、上边线与上边距都对得上。

「方向」全站一个定义：**previous = 更早发布的那个**（专辑、文章皆然）。

### 19.4 验证

- 列表页：3 张卡、每张都有摘要与标签、链接都指向文章页、卡间分隔线恰好 n−1 条、
  窄屏无横向溢出。
- 详情页：正文渲染 10 段 / 3 个小标题、返回链接指向列表、
  正文里的 `../albums/GKCD-003/` 在预览里被改写成 `./GKCD-003.html`（相对路径没被吃半截）、
  上下篇**方向**正确（「上一篇」的日期早于当前、「下一篇」晚于当前）、
  有 `BlogPosting` 结构化数据、头部与正文左边界对齐、正文列宽收到 `--w-text`。
- 变异体：把无封面卡片强行按「左图右文」排（`grid-template-columns: 220px …`）
  → 正文宽 1120 → **220**，判定如实 FAIL。
- **在这里抓到一条假断言**：原先写的是「正文左边界与页头标题对齐」，
  它在变异体下**照样通过** —— 无封面时图位那一格本就是空的，正文会自动落进第一格，
  左边界自然对齐。会露馅的只有**宽度**。断言要挑真的会变的那个几何量，
  写完必须拿变异体验一遍。

---

## 20. 首页主视觉：看板娘

首页原先只有左侧一栏文字，宽屏下大标题右边整片空着。接入社团看板娘（原画
`src/assets/oc.png`，4252×3071 / 14.6MB）后，版面改成「文字在左、画面在右」。

### 20.1 素材流水线

与封面、徽记同一条原则：**母版进 `src/assets/`，产物进 `public/`**。

```
src/assets/oc.png                 母版（14.6MB，只在仓库里躺着）
public/images/hero/oc-640.webp    24KB
public/images/hero/oc-960.webp    42KB
public/images/hero/oc-1440.webp   80KB
```

`npm run oc`（`scripts/build-oc.mjs`）。三档宽度写在 `src/lib/media.ts` 的
`HERO_WIDTHS`，页面用 `heroArtSrcset()` 出 `srcset`、`heroArtSrc()` 出兜底 `src` ——
**档位与脚本必须成对改**，只改一处页面就会引用到不存在的文件。

编码策略与封面共用：`scripts/lib/encode-webp.mjs` 是从 `lib/covers.mjs` 抽出来的
「质量阶梯 q82→70 先行、降尺寸 −10% 兜底」两步让步，封面与主视觉各写一份迟早只会修一处。
抽的时候封面产物逐字节比对过（69 个文件 md5 全等），行为未变。

**刻意不做裁剪。** 构图（人物偏右、长发向左飘）交给 CSS，裁进文件就等于把构图
焊死在素材上，换版式必须重新出图；而输出宽度固定，裁与不裁体积几乎一样。

### 20.2 版位

- 断点 900px。以上两栏（文字 `1fr` / 画面 `0.92fr`），以下单栏、画面排在按钮之后 ——
  首屏第一眼必须是社团名，不能让画面抢在前面。
- 两栏时**纵向居中**而不是底对齐：900–1100px 区间画面比文字栏矮，底对齐会把她的脸
  整张压到标语以下；居中能让脸停在标题那一行。宽屏下两者只差十几个像素。
- 画面窄屏限宽 420px 并居中 —— 横构图的插画拉满整行会又扁又抢戏。

### 20.3 画面与页面怎么接上（这一节是坑）

原画自带一层浅灰纸底（四角采样中位数 **#EFEFEF**）。直接贴到纸白（#FAFBFC）页面上，
无论把边缘遮罩做得多柔，都会看见「一块矩形」—— 差的不是边缘，是**色阶本身**。

做法是两层：

1. `.hero__art::before`：一块与画底**同色**的柔光，四边各外扩 12% / 14% 并用遮罩淡出。
2. `.hero__img`：画面自己只负责两处硬边（左侧长发画到了画布边上、下缘是半身截断），
   用两层遮罩相交淡出；四周也都留了淡出，四角才不会出现直角。

颜色必须与画底完全同色，差几个色阶就会重新露边。所以 `build-oc.mjs` 末尾加了
**底色核对**：采样母版四角的中位数，与 `index.astro` 里 `.hero__art::before` 的
`background` 比对，对不上直接以退出码 1 失败并给出该填的值。换原图忘了同步不再是静默故障。
（核对本身跑过变异体验证：改成 `#eeeeee` → 如实报错并返回 1。）

⚠️ **别改成 `box-shadow`。** 阴影一律被裁在画面矩形框**之外**，而画面的遮罩又把贴边
那一圈淡了出去 —— 两者一错开，交界处会冒出一条比两边都亮的细线。实测该细线是
`242 → 250` 的跳变：缩放后的截图上完全看不出来，是**逐像素扫描**才抓到的。
柔光必须与画面在同一图层里各自衰减。

另：柔光横向比画面多出 12%，窄屏上会探出视口右边；`.hero` 用 `overflow-x: clip`
兜住（不能写 `hidden`，那会把纵向也变成 `auto`，可能凭空多出一条纵向滚动条）。

### 20.4 验证

- **数值扫描**：沿画面左右/上下边缘逐像素打印 RGB，确认色阶**单调连续、没有跳变**。
  这是本节唯一抓住 box-shadow 亮缝的手段（缩放截图与肉眼看不出 8 个色阶）。
- **几何探针**（390 / 512 / 900 / 1100 / 1360）：无横向溢出、`img.complete` 为真、
  画面右边界未越界、遮罩 `mask-composite` 解析为 `intersect`。
- 截图核对 1360 / 900 / 390 三档，确认脸与标题、标语、按钮的相对位置。

---

## 21. M3 · 关于 `/about`

需求 §2.6（成员）、§2.7（社团简介 + 联系与社交入口）、§7（转载规约）**合并成 `/about` 一页**；
联系方式单独留在 `/contact`（见 §22）。参照同人音乐社团的常见做法 ——
这些回答的都是「这个社团是什么」，拆成多个栏目只会让导航更长、每页都更薄。

**2026-09-14 追加**：§2.7 那条「联系与社交入口」没有进 `/contact`，而是在成员与规约之间
立了一块「链接聚合」（`#links`）—— 入口清单是「点进去就完事」，而 `/contact` 要回答的是
「怎么找我们谈合作」，两件事的形状不同。分工见 §21.7。

页脚那条「转载规约」跟着指到 `/about#terms`：**规约的文案全站只有一份**，
不再为它单开一页。（这也意味着日后若真要把它独立出去，把
`src/content/pages/terms.md` 挂到一个新路由上即可，正文一个字都不用改。）

### 21.1 四块内容的来源刻意不同

| 区块 | 来源 | 为什么 |
|---|---|---|
| 社团简介、转载规约 | 集合 `pages` 的两个 Markdown（`src/content/pages/*.md`） | 成篇长文。写进 `.astro` 等于把要长期维护的中文放进模板里，改一句话要动组件 —— 而规约恰恰是改得最勤的那类文案 |
| 成员 | 集合 `members`（`src/content/members/*.md`）+ `MemberCard` | 结构化条目。卡片要的是「昵称 / 担当 / 外链」三个字段，塞进段落里就得靠人肉排版，加一个人准排歪 |
| 链接聚合 | 集合 `links`（`src/content/links/*.md`）+ `LinkCard` | 同理：平台名 / 账号 / 外链是三个字段，写成段落就没法给出「一键复制群号」这个动作 |

**「能推导的就不存」在这里的落点是：成员与入口都不写进简介正文。**
简介里只讲社团，谁是谁由成员卡回答、去哪儿找由链接卡回答；两处都写，就等着两处对不上。

### 21.2 成员状态：两个布尔 → 三值枚举

此前是 `Member` 接口上的 `left` / `guest` 两个布尔，它们的合法组合有 4 种，
而实际只有 3 种含义 ——「既退出、又是合作」是个填得出来、却没有意义的状态。
现在收敛为：

```ts
export const MEMBER_STATUS = ['正式', '合作', '退出'] as const;
```

**这三个字就是页面上显示的字**（卡片标记与分组标题共用 `MEMBER_GROUP_LABEL`），
措辞因此是面向读者的：`客座 · 合作` → `合作`、`离队` → `退出`（2026-09-14 改）。
「客座」是行业内部说法，「离队」带军旅味 —— 都不是社团自己会用的词；
枚举值即展示文案，也省掉了「枚举值 + 另写一份映射表」的二次翻译。

与 `ALBUM_TYPES` / `NEWS_TAGS` 同一条原则：**枚举是唯一真源**，schema 引用它校验，
`/about` 的分组顺序 = 声明顺序，加一种状态只改 `lib/types.ts` 一处。
`Member` 接口本身也一并删掉 —— 形状的真源是内容集合的 schema，手写第二份迟早对不上
（与 `Post` 那次同理）。

**排序用 `order`（同组内小的在前），不按时间。** 社员的先后没有天然依据 ——
社团不维护加入日期，硬凑「按拼音」或「按文件名」都不如直接给一个数省事；
`getMembers()` 末尾再按 id 兜底，两个人 `order` 撞了也有确定的先后。

### 21.3 正式是默认状态，所以它什么都不挂

- **卡片**：正式成员不挂状态标记，只有合作 / 退出有；
- **分组标题**：只有非正式那两组画一行标题。

理由同 §13.3「只有正式那一种带强调色」：**多数派不需要被标出来**。
给每个人都挂一个「正式成员」，等于在暗示「别人不算成员」；
分组标题同理 —— 区块标题已经叫「成员」，第一组再写一遍「成员」是纯重复
（顺带也让「只有一组正式成员」的情况不会出现两行同样的字）。

### 21.4 合并页必须给目录，锚点还得给粘顶页头让位

页头下面一排 `#biography / #members / #links / #terms`。合并页不给跳转，读者为了找规约
要整页滚一遍 —— 那是合并的代价，得在原地还掉。

`#biography, #members, #links, #terms { scroll-margin-top: 80px }`。页头是粘顶的（72px），
不留这一手，跳过去的标题会**藏在导航下面**：点了有反应、但看不全，
属于最难被当成 bug 的那类故障。80px = 72px 页头 + 一点呼吸。
**新增区块必须在那串选择器里补上自己的 id** —— 漏了不报错，只会「点了看不全」。

### 21.5 版位

- 两段长文收进 `--w-text`（680px）。**左对齐、不居中** —— 区块左边界要与其他区块对齐
  （探针断言「正文左边界 == 同区块标题左边界」）。
- 链接卡网格 `repeat(auto-fill, minmax(min(100%, 240px), 1fr))`，比成员卡窄一档：
  卡里只有两三行短文字，给 300px 会变成一行一个、还拉得很长。
- 成员网格 `repeat(auto-fill, minmax(min(100%, 300px), 1fr))`。
  用 `min()` 而不是裸的 `300px`：容器比 300px 还窄时（极窄屏），裸的 `minmax`
  会撑出一个 300px 的轨道，整页横向溢出 —— 同一个坑在样张页的 `minmax(320px, 1fr)` 上也存在。
  用 `auto-fill` 而不是 `auto-fit`：卡片宽度不随人数变化，3 个人和 5 个人看起来是同一套版。
- 成员卡走 `Reveal`。这里没有筛选，不存在 §13.4 的「隐藏态落不到版位上」问题 ——
  但如果日后给成员加筛选，必须先把网格项收归本页模板所有。

### 21.6 验证

无头探针，1360 与 512 两档，断言几何（`design/preview/_about-*.html`，验完即删）：

- 目录三条链接**都有落点**（锚点写错是静默的：点了没反应）；
- 三块内容的 y 递增、成员卡 = 3、状态标记 = 2（正式那张没有标记）、分组标题只有一行；
- 每组网格的首卡贴回自己的原点、卡片不越出容器、横向溢出 = 0；
- 两段正文宽 = 680（512 下 461 = 视口 − 两侧 gutter）；
- 真实点击「转载规约」→ `hash=#terms`、落点 y = 80、未被页头遮挡；
- 页脚那条规约链接在预览里被改写成 `./about.html#terms`（锚点没被吃掉）。

**变异验证（三条断言各自都证明过会失败）：**

| 注入 | 结果 |
|---|---|
| `.about__body{max-width:none!important}` | 正文宽 680 → **1120**，如实 FAIL |
| `#biography,#members,#terms{scroll-margin-top:0!important}` | 落点 y 80 → **0**，未遮挡 false |
| `.grid-members > *:first-child{margin-left:40px!important}` | 首卡左 109 → **149**，贴原点 false |

**链接聚合这一块（2026-09-14 追加）**，1360 / 512 两档 14 条断言：目录四项都有落点、
四块 y 递增、分组标题顺序 = 枚举声明顺序、三组首卡都贴组原点、骨架态 7 张全是「待补充」、
没有 `handle` 就不给复制按钮、没有 `href` 就没有可点的卡、页脚仍是「待补充」、
真实点击目录「链接聚合」→ `hash=#links` 且落点 y = 80、横向溢出 0。
另放两条临时条目把**两种卡片形态**跑全（有 `href` 的整卡是 `<a>` 且不给按钮、
只有群号的给按钮且 `data-copy` 与显示文本一致），并用 mock 剪贴板跑复制交互：
有权限 → 「已复制」且写进去的就是那几个数字；无权限 → 「请手动复制」且号码被选中。

变异体三份各自如实 FAIL：`#links{scroll-margin-top:0}` → 落点 80 → **0**；
把首组标题改成「群组」→ 顺序断言 FAIL；把按钮的 `data-copy` 改坏 → 号码断言 FAIL
（证明读的是属性，不是渲染文字）。另把 `LINK_GROUPS` 的声明顺序临时调换一次再跑，
页面分组顺序跟着变 —— **顺序确实来自枚举，不是巧合成真**。

**探针自身踩到的一处**（记下来，省得下次再误判）：首轮量「首卡是否贴原点」报 `false`，
一度像是网格有内边距。实际是 `Reveal` 还没被 IntersectionObserver 触发，
`.reveal` 上挂着 `translateY(14px)`，把子元素的矩形整体推下去了 14px ——
**量到的 14px 是入场动画，不是版位**。修法是在探针里先注入
`.reveal{transform:none!important;transition:none!important}` 再量（与关掉平滑滚动同理，
都是测试台调整，不是页面行为）。

### 21.7 链接聚合：社媒与群组是「内容」，不是配置

需求 §2.7 里「QQ群号、微博、B站、X、邮箱、Discord」原本打算落在 `/contact`。实际做的时候
挪到了 `/about#links`，理由是**它和「联系」不是一回事**：入口清单要的是一眼扫完、点进去就完事；
而 `/contact` 要回答「怎么找我们谈合作」—— 委托范围、回复时限，那是一篇文案。

- **来源**：集合 `links`（一个平台一个文件、只有 frontmatter）。与成员同一套理由：
  卡片要的是字段，不是段落；「加一个平台」= 放一个文件。
- **分组**：`LINK_GROUPS = ['社交平台', '音乐与发布平台', '群组']`（`lib/types.ts`）。
  与 `MEMBER_STATUS` 同一条原则：**取值即页面上的字**（这里就是分组标题），顺序 = 声明顺序。
  `group` 刻意**必填、不给默认值** —— 漏填的条目不属于任何一组，等于从页面上静默消失，
  宁可构建报错。
- **卡片三种形态由数据自己决定**，不额外加字段：

  | 数据 | 卡片 | 为什么 |
  |---|---|---|
  | 有 `href` | 整张卡是外链，右上角一枚 `↗` | 链接就该整块可点，不用去瞄一个小图标 |
  | 只有 `handle` | 普通卡 + 「复制」按钮 | QQ 群号跳不过去，但九位数字抄错一位就加不进去 |
  | 两个都空 | `handle` 位显示「待补充」 | 素材未到位时先立骨架，填上值自动变成上面两种 |

  「有 `href` 就不给复制按钮」不是省事：按钮嵌在 `<a>` 里本来就是非法嵌套，而链接自身
  已经能把读者带走。
- **复制按钮的失败路径**：`navigator.clipboard` 拿不到时（`file://`、非 https、浏览器拒绝）
  **退化成「选中那串号码」**并提示「请手动复制」—— 一个按了没反应的按钮比没有按钮更糟。
- **页脚「Elsewhere」与这块同源**（都读 `links`，见 `lib/links.ts` 的 `externalLinks()`），
  但**只取有 `href` 的**：页脚在每一页底部，一份抄不走的群号摆在那儿既点不动又占地方。
  同源的直接好处是**不可能对不上** —— 这里加一个平台，页脚一起出现。
  原先 `site.ts` 里那个 href 全空的 `SOCIAL` 数组（页脚因此常年显示「待补充」）就此删除：
  同一件事有第二个来源，迟早两处对不上。
- **YAML 的坑（实测踩到）**：`handle: 123456789` 不加引号在 YAML 里是**数字**，
  纯 `z.string()` 会报 `Expected type "string", received "number"` —— 而群号恰恰最容易被这么写。
  schema 收 `string` 或 `number` 再 `String()`：显示与复制要的都是那几个字符，不存在歧义。

## 22. M3 · 联系 `/contact`

需求 §2.8 从立项起就只有标题、没有条目。真正动手时照的是 §2.7 剩下的两条：
「邮箱这类谈合作的入口」与「是否承接合作 / 委托」。于是这页就两块 ——
**联系方式**（入口卡）与**合作与委托**（说明）。

### 22.1 与 `/about#links` 的边界

两页都在收「入口」，但回答的问题不同：

| | `/about#links` 链接聚合 | `/contact` 联系方式 |
|---|---|---|
| 回答 | 去哪儿找我们玩 | 怎么找我们谈事 |
| 内容 | 社媒 / 发布平台 / 群组的**清单** | 可数的几个入口 + 一段委托说明 |
| 集合 | `links`（**必须分组**） | `contacts`（平铺） |

字段形状明明一样，还是分成两个集合：合成一个就得再加个 `kind` 之类去筛，
而那个字段错填的后果是**静默的** —— 入口从某一页消失，构建照样成功。
共享的是**字段定义**（`content.config.ts` 里的 `entrySchema`）与**卡片组件**（`LinkCard`），
不是集合本身。

### 22.2 顺序：联系方式在前

点进「联系」的人十有八九就是要那个地址。先摆四段说明再给邮箱，等于让每个读者先交一次阅读税。
所以顺序是：**页头 → 联系方式 → 合作与委托**。说明紧跟在下面，真要写委托的人自然会往下看；
反过来则不然。

页头因此**不给本页目录**（与 `/about` 不同）：两块的页，第二块就在下一屏，
不存在「为了找某块要整页滚一遍」的问题 —— 给目录反而会让人以为这页很长。

### 22.3 邮箱只填 `handle`，不填 `href`

看着别扭，但是刻意的：别的入口有外链就填 `href`，邮箱偏不填。两条理由叠加 ——

1. `mailto:` 在网页邮箱环境（Gmail / QQ 邮箱的网页版）里经常没反应。点一下什么都不发生，
   读者会以为坏了；
2. `LinkCard` 的规则是「有 `href` 就不渲染复制按钮」（按钮嵌在 `<a>` 里本来就是非法嵌套），
   而邮箱最需要的恰恰是**复制** —— 手抄邮箱必错，复制下来粘到哪儿都好使。

结论是「只给可复制的纯文本」。`src/content/contacts/email.md` 的注释里写下了这段理由，
免得日后有人「顺手把 mailto 补上」。

### 22.4 复制逻辑抽成 `CopyScript`

那段 `is:inline` 的复制脚本原先写在 `/about` 页面末尾。`/contact` 要用同一套，
于是抽成 `components/CopyScript.astro`，两页各挂一次 `<CopyScript />`。

**不能放进 `LinkCard` 自己**：组件里写 `is:inline` 是**用几次渲染几份**，同一段监听会被注册 N 遍。
也**不能改成普通 `<script>`**：Astro 会把它打包成 `type="module"` 的外链脚本，
而模块脚本在 `file://` 下会被 CORS 拦掉 —— 离线预览（`design/preview/*.html`）里的复制按钮
就静默失灵了，而那正是本仓库唯一能点着看的产物。
组件里带了个 `window.__gekkaCopyBound` 幂等哨兵，万一被挂两次也只注册一次。

### 22.5 `.grid-links` 升成全局工具类

入口卡网格原先写在 `/about` 的 scoped 样式里。`/contact` 要用同一套 ——
复制一份就是「改一处忘另一处」，两个页面的卡片宽度会悄悄分叉。
所以升到 `styles/global.css` 的通用原子类区，两页共用，`/about` 里那份删掉。

### 22.6 社交平台也能是联系方式：地址两处各存一份

`/contact` 的联系方式里除了邮箱，还放了 **X（原 Twitter）** 与 **Bilibili** ——
同人社团谈委托，私信往往比邮件先到。同一个 B站 空间因此在两处出现：
`/about#links` 那份说的是「来看作品」，`/contact` 这份说的是「来谈事」，`note` 不同。

地址是**各存一份**的（`content/links/bilibili.md` 与 `content/contacts/bilibili.md` 各写一遍）。
看着像冗余，但与 §22.1 是同一条道理：不做跨集合的隐式引用 —— 那种引用字段填错是**静默的**
（卡片直接不出现，构建照样成功），而「换账号要改两个文件」是改的时候看得见的。
两个文件里都写下了这句提醒。

X 的 `href` 暂时空着 → 卡片显示「待补充」；填上即生效，不用动代码。

### 22.7 注释里别写 HTML 标签的字面量

症状：`npm run dev` 冷启动打出 `[vite] Failed to run dependency scan` 与一段
`PARSE_ERROR`，指向 `src/components/CopyScript.astro`，说某行「Expected a semicolon」。
真凶是**注释里写的标签字面量**（`CopyScript.astro` 与 `TrackList.astro` 各一处，
写的是「别改成普通的 X 标签」那句话里的开标签）。

机制：依赖扫描把每个 `.astro` 当入口整体切一遍，切分只看标签、不看 frontmatter 边界 ——
注释里那个**开标签**同样被当成「脚本开始」，从那里到文件末尾的注释被拿去当 JS 解析，
于是语法错。**闭标签**更凶：HTML 里脚本内容就是以第一个闭标签收尾的，
写在注释里会把 `is:inline` 的脚本当场切断（页面不报错，功能静默消失）。

规避：要指代它们，写「普通脚本」「页面末尾」这类说法。同一条也适用于**探针的注入点** ——
别用 `replace('</body>', ...)`，脚本注释里出现过它，命中的是**第一处**（在脚本块内），
注入的探针会被当成脚本内容，页面被切断，而探针只表现为「取不到结果」。

> 缓存会骗人：扫描成功过之后 Vite 把结果写进 `node_modules/.vite`，之后再启动就**不再扫描**，
> 于是同样的代码「今天好了」。要复现先删掉那个目录。

### 22.8 验证

跑了**两轮**：干净态，以及带两条临时条目（一个只有 `handle` 的邮箱、一个只有 `href` 的平台）。
临时条目那一轮是必要的 —— 干净态只有一张「待补充」的骨架卡，复制按钮根本不会渲染，跑不到。

- **干净态**：`/contact` 15 条 + `/about` 回归 13 条，全 PASS。覆盖：两区块存在且联系方式在前、
  卡片数与骨架态文案、骨架态无复制按钮、骨架卡不可点、网格 ≥ 3 列且每列 ≥ 240px、
  「**没有邮箱时 JSON-LD 里不出现 `contactPoint`**」、正文列宽 680、无分组标题、横向溢出 0。
- **临时条目一轮**：`/contact` 30 条全 PASS。除上述外还跑到：两种卡片形态（有 `href` 整卡是 `<a>`
  且不给按钮 / 只有 `handle` 给按钮）、`data-copy` 与卡上显示一致、复制脚本只注入一份、
  `ContactPage` 与 `Organization.contactPoint.email`、点复制（mock 剪贴板）写进去的就是那个地址、
  按钮文案变「已复制」并自动复位、**拿不到剪贴板时文案变「请手动复制」且号码真的被选中**、
  点 `#channels` 落点 y = 80。
- **五份变异体各自如实 FAIL**：拿掉 `.grid-links` 规则（两页各一次，网格退回 1 列）、
  `scroll-margin-top` 改 0（落点 80 → **0**）、`data-copy` 改坏（属性与显示不一致）、
  JSON-LD 的 `email` 塞成 QQ 群号（那条断言 FAIL —— 证明它真在挑「像邮箱的」。）

**补记（2026-09-15，联系方式加上 X 与 B站 之后）**：重跑一轮 —— 干净态 `/contact`
18 条（1360）/ 17 条（512）全 PASS，临时条目一轮 23 / 22 条全 PASS；
三份变异体各自如实 FAIL（抹掉 B站 的 `href` → 「外链卡指向 B站空间」红；
`.grid-links` 打成 1 列 → 「三张卡排在同一行」红；删掉 X 卡的 `note` → 那条断言红）。
这一轮把「网格 = 3 列」改掉了：`.grid-links` 是 `auto-fill`，1338px 下会排出 **4 条轨道**，
写死「= 3」是断言错、不是布局错 —— 改成「同一行 + 等宽 + 不低于 240px」。

**另记三个踩到的坑**：

1. 探针源码的注释里写了标记元素的**字面量**，取结果的脚本用非贪婪匹配会**抢先命中注释里那一处**，
   于是拿到的「结果」是注释的后半句，`JSON.parse` 直接报 `Unexpected token`。
   取的时候要从后往前找第一个能解析的候选，注释里也别写那个字面量。
2. `--window-size=1360` 下 `window.innerWidth` 实测是 **1338**（扣掉滚动条），
   断言里别写死视口数值，写「够宽」并把它打进明细。
3. **探针的注入点别用 `replace('</body>', ...)`**：页面内联脚本的注释里出现过它，
   `replace` 命中的是**第一处**（在脚本块里），注入的探针于是被当脚本内容切掉，
   页面坏掉而探针一声不响地取不到结果。用 `lastIndexOf`。

## 23. M3 · 站内搜索 `/search`

需求 §4.8 一直是空的（P1）。做法是**构建时把全部条目排进 HTML、客户端只做过与不过的判定**，
与曲目库 / 动态的筛选同一套 —— 连 `data-search` 属性名都一样。

### 23.1 为什么不用 Pagefind / Fuse.js

需求里点名了这两个，最后都没用：

1. **索引能从内容集合推导**。标题、日期、链接全部现场从集合算出来（§13.1「能推导的就不存」）；
   引一个搜索库等于给同一份数据再养一份副本，而副本是要有人负责同步的。
2. **这个体量用不着全文索引**。164 条，子串匹配一遍是同步的、零感知的延迟；
   倒排索引换来的那点速度，这里量不到。
3. **不多一个二进制依赖**。Pagefind 是一个 Rust 二进制（安装时去 GitHub 拉），
   与「全站自托管、不依赖第三方域名」这条方向也不一致。
4. **离线预览照常能用**（`design/preview/search.html` 就是同一份 HTML，脚本内联）。

代价是**没有模糊匹配与拼写纠错**。中文查询本身就是连续子串，这一条落不到实处；
真要做加权排序，改的也只有 `lib/search.ts` 里 `search` 那一串的拼法。

体量：164 条 / 原始 126 KB / gzip **29.6 KB**（同页 `/tracks` 的 gzip 是 24.4 KB，作参照）。
开销主要是 127 条曲目的检索串加三篇长文的正文 —— 换掉了「一次搜索请求」。

### 23.2 枚举是唯一真源，这次是 `SEARCH_KINDS`

`lib/types.ts` 的 `SEARCH_KINDS = ['专辑','曲目','杂谈','动态','成员','说明']`：
取值就是页面上的字（结果分组标题与范围芯片共用一份），**声明顺序就是分组顺序**。
`lib/search.ts` 里那张表写成 `Record<SearchKind, SearchHit[]>` —— 枚举里加一个取值，
那里会当场编译不过：「加了枚举却忘了往搜索里接」不能是静默的。

### 23.3 谁进检索串、谁不进

| 类别 | 主文字 | 右侧次要信息 | 检索串里有什么 |
|---|---|---|---|
| 专辑 | 标题 | 编号 · 发布日 | 标题 / 别名 / 副标题 / 编号 / 类型 / 简介 / 标签 / Staff 姓名与角色 |
| 曲目 | 曲名 | 编号 · 专辑名 | 复用 `lib/tracks.ts` 拼好的那串（曲名 / 原曲 / 出处 / 编曲 / 演唱 / 专辑） |
| 杂谈 | 标题 | 日期 · 标签 | 标题 / 标签 / **正文全文** |
| 动态 | 正文首段 | 日期 · 标签 | 标签 / 日期 / **正文全文** |
| 成员 | 姓名 | 担当 | 姓名 / 担当 / 一句话 |
| 说明 | 文案标题 | 所属栏目 | 标题 / 文件名 / **正文全文** |

三条刻意的取舍：

- **曲目不进专辑的检索串**。每首曲目自己就是一条结果；再挂到专辑上，会让「搜某首曲子」
  同时冒出专辑与曲目两条重复结果。
- **长文进全文**（杂谈 / 动态 / 说明）。这是搜索的主要价值（「那篇讲混音的文章」）。
  代价是搜索页会随文章一起变长 —— 真到拖慢页面的那天，正确的做法是把索引挪去构建产出的
  JSON 里按需取，而**不是**把正文从检索里砍掉。
- **正文不进页面**。结果行只有「标题 + 一行次要信息」。这一页的职责是把人送到正确的那一页，
  不是做成第二个列表页。

### 23.4 `pages` 的去处：一处写死 + 构建期双向断言

其余链接全部由集合推导（`albumHref` / `postHref` / `#members`），只有 `pages` 例外 ——
成篇文案是内容、藏在 Markdown 里，「它显示在哪一页的哪一节」没有别的来源可推。
所以 `lib/search.ts` 里有一张 `PAGE_TARGETS`，并且**两个方向都卡**：漏登记一篇、
或登记一个不存在的 id，都构建失败。不卡的话，漏掉的那篇会静默地搜不到
（页面照常渲染，只是没人找得到它）。

### 23.5 结果页：没有 JS 时它是一份完整索引

- 搜索框与范围芯片**静态带 `hidden`**，由脚本打开 —— 摆一个搜不动的输入框只会让人以为坏了。
- 164 条结果行全部由服务端排进 HTML（分组顺序 = 枚举顺序，组内顺序沿用各集合本来的顺序），
  客户端只把不合格的行 `hidden` 掉、把「一条不剩」的分组整组收起。**排序规则只有一份**。
- 计数文案（`共 N 条` ↔ `显示 N / M 条`）与「清除筛选」沿用曲目库那一套；
  `.input` / `.toolbar*` / `.filters__search` 一并从 `/tracks` 的 scoped 样式**上移到
  `global.css`**（同 §22.5 把 `.grid-links` 升成全局工具类的理由：两页各写一份，
  改一处忘另一处，同一个输入框会在两处悄悄分叉）。
- `?q=` 是入口：带进来直接筛好，并同步回地址栏（可刷新、可分享）。`file://` 下改地址栏
  被浏览器拒掉时静默跳过（`history.replaceState` 包了 try），离线预览不受影响。
- 焦点只在**细指针**设备上自动落进输入框，带 `?q=` 进来时也不抢 ——
  触屏上抢焦点会当场弹出软键盘，把刚打开的页面顶掉半屏。
- `noindex`：这一页把全站内容列了一遍，对搜索引擎是重复内容。它是工具，不是内容。
- 页头入口放在**尾巴（右侧）**，不进 `NAV`：`NAV` 是内容地图（栏目），搜索是工具 ——
  混进去导航会变成七项，而它也没有「当前栏目」这回事。窄屏也留着（那里本来只有一颗汉堡按钮）。

### 23.6 动态结果要能落到那一条

六条动态原本没有 `id`，所有结果只能指到 `/news` 页首，等于搜不出来。
`Timeline.astro` 给每条补 `id={entry.id}`（文件名即 URL 片段），并加
`scroll-margin-top: 88px` —— 页头粘顶 72px，不留这一手，跳过去时日期与正文会被压在导航下面
（同 §21.4）。

### 23.7 验证

无头 Chrome 探针，追加到产物最末、单轮跑完一次写入。**1360 与 512 两档各 39 条断言全 PASS**
（另加 `?q=` 入口一轮 36 条、强制「减少动态效果」一轮 30 条）。关键几条不是「数个数」：

- **与参考实现逐条对账**：探针不看 DOM 的显隐状态，只按 `data-search` 自己算一遍「谁该出现」，
  再与真正可见的那批**比对同一批节点**。六组查询（`mriya` / `东方` / `khrisma` / `gkcd-013` /
  `khrisma 东方` / `zzz-没有这个词`）各自同时核对命中集合、分组收起、计数文案、空态、
  被藏行不占位 —— 只数条数是抓不到「位置留空洞」的。
- **几何**：筛完之后首个可见行贴住列表原点（实测 `top - 列表top = 0.0`）、末尾行贴住下沿、
  相邻可见行间距 0 ——「被筛掉的位置真的退出了布局」，而不只是看不见。
- **顺序真来自枚举**：把 `SEARCH_KINDS` 的声明顺序临时调换再构建，页面分组顺序跟着变成
  `曲目,专辑,杂谈,动态,成员,说明`，那条断言如实 FAIL。
- **两份变异体各自如实 FAIL**：① 让被筛掉的行 `display: block` 继续占位 → 7 条红，
  几何明细从 0.0 变成 **779px 的空洞**；② 出场标记下把幕布摁回 `opacity: 0` → 那条红。
- **动态深链**：搜索页里 6 个锚点全都能在 `/news` 产物里找到，并真跳一次 ——
  条目落点 `top = 87.7`、页头高 73px（在导航下方，没被压住）。
- **无 JS 退化**：产物里 164 行**没有一行**静态带 `hidden`，筛选块与清除按钮静态带 `hidden`。

## 24. 页面切换过渡

需求 §5 勾的是「中等（滚动触发、页面转场）」，而 §6 里写着「不做页面转场动画」——
两处本来就顶着。2026-09-15 按需求做，但**只做透明度**。

### 24.1 一页一半，都不需要往页面里加标记

| 半场 | 在哪 | 做什么 |
|---|---|---|
| 出场 | `BaseLayout.astro` 页尾脚本 + `global.css` 的 `.page-fade` | 点站内链接 → 纸色幕布淡入（180ms = `--dur-fast`）→ 再跳转 |
| 入场 | `global.css` 的 `body { animation: page-enter }` | 每次文档加载，内容从透明淡进（320ms = `--dur`） |

两半接得上，靠的是**三处底色是同一个 `--paper-50`**：幕布色、`html` 底色（新页面 body
还是透明时露出的那层）、`body` 底色。少一处就会「闪一下别的颜色」，所以 `html` 也补了底色。

### 24.2 为什么不用 Astro 的 ClientRouter（View Transitions）

技术上更省事，但它是**接管整份文档**的：换页之后页面里的 `is:inline` 脚本**不会重跑** ——
本项目的滚动淡入观察器、溢出文字测量、曲目库筛选、复制按钮全都在 `is:inline` 里，
每一处都得补 `data-astro-rerun` 或改挂 `astro:page-load`，是一轮铺开到全站的改动，
而漏掉任何一处的症状都是「某个页面的某个功能静默失效」。

自己写的这半场只做两件事：拦截点击 + 切一个类名。**每次跳转仍是真跳转**，既有脚本照常执行。

### 24.3 只用 opacity，不用位移

位移（`transform`）会给 `body` 造出一个包含块，把粘顶页头与 `position: fixed` 的元素一起带歪；
而且滑来滑去与「清冷极简」也不搭。这与 §6「不做首页动效大场面」是同一套判断。

`animation-fill-mode: both` 是关键：没有它，动画开始前会有一帧是「内容已经全亮着」，
那一帧就是闪。

### 24.4 只接管站内的页面跳转

外链与 `mailto`（拦了也没用 —— 别人的页面不会配合淡入）、同页锚点（那是页内滚动，
拉一次幕布反而像卡了一下）、修饰键与中键、`target=_blank`、`download` —— 一律不拦。

出场时长从令牌现读，样式与脚本不各写一个数字 —— 但**不能直接 `parseFloat` 就完事**：
自定义属性取回来的是「写出来的那个字符串」，而构建时的压缩器会把 `180ms` 压成 `.18s`，
`parseFloat('.18s')` 得到 `0.18`，当成毫秒用就差一千倍 —— 幕布刚点亮就跳走，这半场等于没跑。
这个 bug 在产物里真的发生过、dev 下完全看不出来，所以读法固定为 `msOf()`（按单位算）：
`Intro.astro` 里另有一份同样的 helper（两个 `is:inline` 脚本没法共用模块，打包成 module 会在
`file://` 下被 CORS 拦 —— §22.4 同一条理由），**改一处记得改另一处**。两个兜底：

- **跳转没走成**（被拦、目标被删、链接是个空壳）时 1.2s 后自己把幕布收回 ——
  否则整页被一块纸色永久盖住，而且没有任何出口；
- **从 bfcache 回来**时页面是原样恢复的、脚本不会再跑一遍，于是「点了链接又按后退」
  会停在盖着幕布的状态里（整页空白）。`pageshow` 里把标记撤掉。

### 24.5 验证

同一套探针（1360 档）：

- 入场：`animation-name = page-enter`、`0.32s`、`fill-mode = both`；
- **幕布色 = `html` 底色 = `body` 底色**（三个 `rgb(250, 251, 252)`）——「接得上」的判据；
- **负向对照两条**：页内锚点、外链都不被接管、也不拉幕布（`prevented=false pt=undefined`）；
- **正向一条**：站内链接被接管（`prevented=true`）且标记挂上，并且标记下幕布**真的匹配到那条
  把它点亮的选择器**（`matches=true`、目标不透明度 `1`）→ 1.4s 后兜底把它收回；
- 强制 `--force-prefers-reduced-motion` 另跑一轮：入场动画为 `none`、站内跳转不拉幕布。

> **两处「量错了地方」，记下来免得再犯**：① 虚拟时间下 `setTimeout` 走虚拟钟、CSS 过渡走真实钟，
> 所以「睡 300ms 再读 `opacity`」永远是 `0` —— 看着像幕布没亮，其实是量错了东西；
> 改成把过渡临时关掉、读**级联终值**。② 这轮窗口 1360，`innerWidth` 实测 **1338**（老坑）。

---

## 25. 首页进场遮罩

「进入网站」时挡在首屏上的一块纸色，等文档就绪再淡出，露出首页主视觉。
版式与手法照两个参考站的首屏 —— 它们其实是同一套：

| | logical-emotion.com | jizue.com | 本站 |
|---|---|---|---|
| 遮罩 | 纯白 `#fff`，`fixed`，`z-index: 99999` | 纯白 `#fff`，`fixed`，`z-index: 9999` | `--paper-50`（与页面底色同一个 token） |
| 指示物 | `lds-ellipsis`：四个 11px 黑点、0.6s 循环 | 一张居中的 `loading.svg` | 一条 1px 的弧，从 12 点起笔画满整圈 |
| 退场 | `on('load')` → `fadeOut('slow')` 后 `remove()` | `on('load orientationchange resize')` → `fadeOut(1000)` | `load` **且**已亮满 `--intro-ring-dur + --intro-hold`（1200ms）才退；退场是**三拍**共 700ms，之后摘节点 |
| 主内容 | wow.js `fadeInUp` + `data-delay` 100/300/500ms | 滚动触发 `.mvup` | 第三拍：120ms 起、每档 70ms —— **比纸面更早起步**（§25.4）|

### 25.1 指示物为什么不是那几个跳动的点

跳点是通用 loading 的语汇，它说的是「后台在忙」；而这个遮罩的场景不是「忙」，是「开场」。
换成一条 1px 的弧从无画到整圈：弧本身就是「月」的轮廓，对得上社名；1px 细线、无填充，
对得上「清冷极简」。48px 的圆、`--ink-900`，画面里只有它在动。

圆上的 `pathLength="1"` 把周长归一化，于是 `stroke-dasharray` / `stroke-dashoffset` 都用 `1`
这个无量纲的数 —— 不必把 `2πr = 138.23` 写死进样式，改半径不用同步改两个数字。
（产物会被压缩成 `1px`；在 48×48 的 viewBox 与同尺寸显示框下两者等价，但**别把这个 SVG
改成响应式尺寸**，那时 `1px` 就不再等于整圈了。）

弧下面跟两行字：站名（18px / 0.28em 字距）与拉丁名（`--text-meta` / `--ink-400`），
分别在弧之后 200ms / 290ms 淡入 —— 有个「先画圆、再落名」的次序。

### 25.2 「要不要播」必须在首帧之前定下来

遮罩是「先亮着、等文档就绪再淡出」的。判断若放在 body 的脚本里就已经晚了：遮罩会先以
「不播」的姿态露一帧首页、再补上，那一下闪比不做还糟。所以三道门都写在 `BaseHead.astro`
的 head 同步脚本里，靠 `html` 上的 `data-intro` 与其余部分接头：

| `data-intro` | 状态 | 谁设的 |
|---|---|---|
| 不存在 | 不播。`.intro` 的默认样式就是 `opacity: 0; visibility: hidden`，它**不存在于视觉中**（禁用 JS 时也是这个状态） | — |
| `in` | 亮着，弧在画 | head 里的门禁脚本 |
| `out` | 正在淡出，同时触发主视觉的错开上浮 | 组件自己的退场脚本 |

三道门：不是首页（编译期就不渲染这一块）、用户开了「减少动态效果」、本会话已经进过站。
第三条用 `sessionStorage`，**读不到就照常放一次** —— 隐私模式 / 沙箱 / 某些 `file://` 场景下，
宁可多放一次，也别让第一次来的人什么都看不到。

整块只在首页渲染（`intro` 为假时连注释都不输出）：别的页面不该为它背上这段脚本。

### 25.3 两条兜底

1. **最短显示时长。** 只等 `load` 不行 —— 本地缓存命中时它来得太快，遮罩闪一下就没，
   比不做更廉价。要 `load` 与「已亮满 `--intro-ring-dur + --intro-hold`」**两个条件都满足**
   才退。弧画完要 1000ms，这个下限顺便保证了「弧总是画完的」。
  这两个数都从样式现读（`msOf()`），**不能 `parseFloat` 直接读** —— 压缩产物里 `--intro-ring-dur`
  是 `1s`、`--intro-hold` 是 `.2s`，那样读出来是 **1.2ms**：弧刚起笔就退场。
2. **最长等待。** `load` 迟迟不来（图挂住、外链被墙）也不能把人锁在遮罩里，2600ms 到点强退；
   head 里另挂一道 8s 兜底，连「退场脚本没跑成」都能自己摘掉节点。宁可没有动效，也不能白屏。

### 25.4 退场：三拍交接

退场**不是一次淡出**，是同一段时间上错开的三拍。非得分拍的理由很具体：全压在一起时会有那么
一会儿 —— 弧、小字站名、纸面底下刚浮出来的大标题，三层东西各带一半透明度叠在同一处，看着就是「糊」。

| 拍 | 谁 | 起 | 时长 | 干什么 |
|---|---|---|---|---|
| ① | 指示物（弧与站名） | 0 | 380ms | 弧放大到 1.34 倍**散开**（像月晕）、两行字上移 10px 淡出 |
| ② | 纸面 `.intro` | 240ms | 460ms | 淡出，露出主视觉 |
| ③ | 主视觉五件（`index.astro`） | 120ms | 每件 380ms、每档错开 70ms | 从下浮上来接住 |

三拍的参数全是 `global.css` 那一节 `:root` 里的令牌（`--intro-leave-dur` / `--intro-veil-delay` /
`--intro-veil-dur` / `--intro-rise-*`），**脚本与 `index.astro` 都从那里读** —— 交接的平滑程度
全在这些毫秒的对齐上，任何一处另写一遍数字迟早会错开。咬合关系（也是探针里断言的那几条）：

- ③ 的起点（120ms）**比 ② 还早**：动作藏在纸后面。等纸面开始揭（240ms）时，主视觉第一件已经
  走了一半，于是「揭开」看到的是**一幅已经就位的画**，而不是「一张白纸、内容再慢慢长出来」。
- ① 走完（380ms）时 ② 才刚走 140ms：指示物不会和大标题在纸面上各半透明地叠着。
- ③ 的最后一件在纸面揭完前落位（780ms 对 700ms）—— 尾巴不拖到纸面之后。

**纸面用自己的缓动**（`--intro-veil-ease`，缓入缓出），是全站唯一不跟 `--ease` 走的一条。
理由是量出来的，不是偏好：`--ease` 是五段缓出 `(0.22, 1, 0.36, 1)`，适合「到位」（东西快速过来
然后稳住），不适合「抬起」—— 拿它揭一整块纸，名义 460ms 的动作在头 60ms 里就走掉一半，剩下
全是极低透明度的拖尾，读起来仍然是「闪一下」。大面积色块要的是均匀。下面是探针逐刻定格量出来的：

| T(ms) | 0 | 150 | 240 | 300 | 380 | 450 | 560 | 700 |
|---|---|---|---|---|---|---|---|---|
| 纸面 | 1.00 | 1.00 | 1.00 | 0.97 | 0.81 | 0.57 | 0.19 | 0 |
| 指示物 | 1.00 | 0.09 | 0.01 | 0 | 0 | 0 | 0 | 0 |
| 主视觉第 1 件 | 0 | 0.33 | 0.85 | 0.95 | 0.99 | 1.00 | 1.00 | 1 |

**退场写成 CSS 动画而不是过渡**，也是刻意的一步：过渡的起点是「当前值」，说不清「从 1 淡到 0
但先等 240ms」；而且动画可以被 `pause()` + `currentTime = T` **定格在任意时刻**（上面那张表就是
这么量出来的），过渡做不到。一个能被逐帧对账的动效，才有资格说自己「平滑」。

还有一条必须留在纸面上的话：`data-intro` 由 `in` 改成 `out` 之后，驱动弧的那条动画选择器不再
匹配、动画被撤掉，`stroke-dashoffset` 会**回落到基础值 1** —— 弧会当着人的面突然消失。所以
`html[data-intro='out'] .intro__ring` 显式写上 `stroke-dashoffset: 0`，让它跟着纸面一起散出去。
同理 `data-intro` **只改不删**：主视觉的错开上浮靠 `html[data-intro='out']` 驱动，删掉属性会让
正在跑的动画当场中断、元素跳回终态。

### 25.5 与页面切换过渡的关系

层级相邻但互不干扰：遮罩 `1000`、页面切换幕布 `999`、粘顶页头 `50`。从首页离场时遮罩
早就摘掉了，幕布不会被它盖住。底色是**同一个 token**（`--paper-50`），遮罩淡出时露出的
`html` 底色、`.page-fade` 幕布色、`body` 底色四者一致，过程中不会闪出第二种白
（§24.1 那条判据从三处扩到四处）。

### 25.6 验证

跑 `design/preview/home.html`（相对路径，`file://` 下资源齐全，`load` 的时序才真实），1360 档，
五轮共 **83 条断言**：

| 轮次 | 断言 | 验的是什么 |
|---|---|---|
| 正常态 | 36 | 三拍的时间轴、关键帧取值、咬合关系、终态、节点自摘 |
| 定格量测 | 16 | 逐刻不透明度（§25.4 那张表）——「顺不顺」就是这张表 |
| `--force-prefers-reduced-motion` | 7 | 不播时遮罩不在视觉里、主视觉一件都没动画、幕布照样收着 |
| 「本会话已进过站」 | 6 | 同上（用桩把 `sessionStorage.getItem` 换成返回 `'1'`） |
| 出场幕布（真点击 + 真跳转） | 18 | 负向对照 6 条 + 接管 3 条 + 时长对账 4 条 + 落页初始态 |

- **时长不是「量」出来的，是「记」下来的。** 在 head 里把 `setTimeout` 包一层，记下脚本算出来的
  每个 delay —— 正常态挂的是 `1200 / 760 / 2600 / 8000`（最短显示 / 摘节点 / 最强兜底 / 门禁兜底）。
  这个环境里时间本身量不准（`setTimeout` 走虚拟钟、`Date.now()` 走真实钟、`performance.now()`
  又是另一回事），但「脚本算出来的数是几」是确定的。
- **九份变异体各自如实 FAIL**：弧在退场时回退（1）、去掉主视觉浮入（2）、退场不淡（1）、
  不播时遮罩仍亮（2）、`MIN_MS` 换回写死的 700（1）、`msOf()` 换回 `parseFloat`（2）、
  纸面缓动换回 `--ease`（3）、纸面延迟抹掉（5）、主视觉延迟抹掉（2）。
  另有一份**只改令牌不改代码**的对照：把 `--intro-hold` 从 200ms 改成 2200ms，退场起点跟着后移，
  其余断言一条不变 —— 证明这个下限真的是从样式推出来的。
- 定格图留在 `design/preview/intro-handoff.png`（六个时刻拼一张），由临时脚本生成后删除。

> **这一轮踩到的两个新坑。**
> ① **`parseFloat(getComputedStyle(...).getPropertyValue('--x'))` 会被压缩器击穿**：自定义属性
> 取回来的是「写出来的那个字符串」，而构建时 `200ms` 被压成 `.2s`、`180ms` 被压成 `.18s`，
> `parseFloat` 得到的是**秒**，当成毫秒用就差一千倍。dev 下一片正常，产物里「到时自己退场」变成
> 「立刻退场」—— 症状是「接缝」而不是报错（§24.4 的 `msOf()`）。而修复前的那版探针**照样 PASS**，
> 因为它只断言了「它退场了」，没断言「不早于多少」；是后来加的那条对账把它揪出来的。
> ② **探针的期望值不能按线性模型写。** 缓动曲线不是线性的：`--ease` 下 79% 的进度视觉上已接近完成，
> 按线性写的「T=700 时最后一件还在收尾」会假红；反过来「纸面在 300ms 还有 0.85」也要先问过曲线。
> 写断言前先想一遍「这条曲线在这个点上大概是多少」。
> ③ 上一轮那条仍然有效：**采样点不能写死**。探针在文档末尾执行，那时虚拟时间已经往前走了一段，
> 固定的「等 900ms 再采样」会越过窗口；改成轮询等状态真的变过去。
---

| 日期 | 内容 |
|---|---|
| 2026-09-12 | M1 初稿：确立清冷极简方向、配色与字体令牌、9 个基础组件、动效规范 |
| 2026-09-12 | M1 定稿：强调色取 A 月白青；字体切换为自托管 MiSans（3 字重 / 300 分片）；新增字形自检与字体署名 |
| 2026-09-12 | M2：内容集合 + zod schema 管专辑数据；dizzylab 抓取脚本（带人工字段合并保护）；专辑列表页与详情页；封面三档 WebP 预生成；JSON-LD MusicAlbum；OG 分享卡 |
| 2026-09-12 | M2 收尾：接入社团正式徽记（裁边母版 + favicon + 分享卡水印）；专辑页删掉「试听与购买」、去掉版本/首发/特典、「发行」改「发布」；原创曲不再标 `note`；多语言入口搁置 |
| 2026-09-12 | 文案与字段第二轮：英文名定稿 Gekka Sound；标语换成正式 slogan；曲目表去掉原曲作曲者；专辑页去掉价格 |
| 2026-09-12 | 第三轮收敛：曲目表删去行尾试听按钮（`listenHref` prop 一并移除，网格收列）；Staff「封面绘画」→「绘画」，抓取脚本加 `ROLE_ALIAS` 归一化 |
| 2026-09-12 | 区分正式 / 非正式：系列由编号推导（不加字段）；`SeriesTag` 标记接到卡片与详情页；列表页新增「系列」筛选；预览导出脚本改为自动发现专辑页 |
| 2026-09-12 | 封面体积治理：编码器改为「质量阶梯 q82→70 先行、降尺寸 −10% 兜底」两步让步，文件名不变以保证 URL 稳定；`npm run covers` 退出码改为「告警不阻断、真失败才阻断」，与本文档对齐并修掉原先的言行不一（§9.3） |
| 2026-09-12 | 修筛选后不重排：原先靠 `:has()` 收外层，被 Astro 样式作用域静默击穿（组件根元素拿不到本页 cid），筛掉的位置留空洞。改为网格项归本页所有（`cell` 在外、`Reveal` 在内），`[hidden]{display:none}` 一条规则生效；验证方式也从「数张数」改为「断言几何 + 变异验证探针会失败」 |
| 2026-09-12 | 曲目行加左右内边距（与垂直同档），悬停灰底不再紧贴序号与时长；用 padding 而非 margin，借 border-box 保住分隔线与标题的左边界对齐 |
| 2026-09-12 | 类型收敛：`东方 Arrange` → `东方Project`，删掉从未使用的「合作企划」；`ALBUM_TYPES` 下沉到 `lib/types.ts` 作为唯一真源（`AlbumType` 由它推导，schema 引用它）；类型筛选芯片改为按枚举声明顺序排，不再随新专辑重排 |
| 2026-09-12 | frontmatter 制表符自动化修复：新增 `scripts/lib/fix-tabs.mjs`（清洗行首 Tab 缩进为 4 空格，只动 frontmatter、不碰正文与字符串里的 Tab）+ `scripts/fix-tabs.mjs` CLI（`npm run fix:tabs`，`--check` 只查不改）；`fetch-dizzylab` 的 `readExisting` 解析前先清洗，抓取永不因人工敲的 Tab 崩掉；`npm run build` 挂 `prebuild` 自动兜底 |
| 2026-09-12 | 曲目表溢出文字悬停滚动：四列（曲名/原曲出处/编曲作曲/演唱）文字比格子长时，默认右侧渐隐遮罩提示、悬停自动横向滚动展示全文。内层 `inline-block` + `transform` 动画（`text-indent` 与外层 transform 两种方案实测均失效，已记录）；省略号换成渐隐遮罩（inline-block 下 ellipsis 不生效）；`prefers-reduced-motion` 不滚、`title` 兜底；窄屏内层退化回换行 |
| 2026-09-13 | M3 起手：新增 **曲目库 `/tracks`**（§17）—— `lib/tracks.ts` 把全部专辑的曲目摊平成跨专辑索引并派生筛选项；6 列索引表（原曲与出处合成一列、增设「收录专辑」与专辑级试听入口）、搜索 + 系列/类型芯片 + 出处作品/编曲者下拉、结果计数与空状态、窄屏摊成卡片、列头粘顶 |
| 2026-09-13 | 溢出滚动机制抽成全站共用：新增 `styles/clip.css`（`.clip` / `.clip-inner`）与 BaseLayout 里的一次性测量脚本，`TrackList` 改为复用、删掉重复实现；筛选条与空状态（`.filters` / `.chip` / `.empty`）从专辑列表页上移到 `global.css`。顺带修掉 3 处出处作品的全角波浪号（同一作品在下拉里裂成两项的静默故障） |
| 2026-09-13 | 曲目库定稿四改：① 删掉简介段；② 系列/类型也改成下拉，四项筛选形态统一、去掉芯片那套选中态与互斥脚本，共用一条 `change` 通道；③ 删掉「演唱」列（127 首仅 2 首有值，列数 6 → 5）；④ 行首编号改为**当前列表的顺序号**（1..N）并随筛选重排（§17.9）。验证仍走无头探针：筛「非正式」后首个可见行由原名次 09 变 01、25 首连续，列头与行五列左边界逐一相同、行缝 0、横向溢出 0 |
| 2026-09-13 | 曲目库再改两处（§17.2 / §17.3 改写）：① **删掉「试听」列** —— 理由与 §12.1 删专辑页行尾试听钮相同（那一格本就指向专辑级入口，专辑页头部已有「试听」主按钮，而「收录专辑」现在又直接通向同一张专辑），`IndexedTrack.listen` 随之删掉，列数 5 → 4；② **「收录专辑」的专辑名改为链接**指向专辑页首页，曲名仍指向 `专辑页#tracks` —— 两个出口粒度不同，不算重复。探针新增**列模型对账**（列头与行 4 格左边界逐一相同、每行恰好 2 个 `<a>` 且两个 `href` 互为「去 `#tracks`」关系），链接断言同样做了变异验证；顺带修掉探针自身的两个坑 —— 注入点必须追加到文件最末（Astro 把页面 `is:inline` 脚本放在 `</html>` **之后**，注入到 `</body>` 前会排在页面脚本之前）、产出改为同步落盘不押在 `fonts.ready`/定时器上 |
| 2026-09-13 | 曲目库计数加「（含 8 首重复乐曲）」（§17.10）：曲名 / 作者 / 所用原曲 / 所属分类四项全等即视为同一首，口径数「多出来的条目」—— 127 − 8 = 119，括号里的数能直接减出实际曲目数。曲名**全等**比对是刻意的：抹掉括号后缀就会把 Remix 并进原版（现有 8 首 Remix 靠标题自带的版本后缀天然区分；7 组重复**全部**跨 GKSL → GKCD 系列，所以「所属分类」取 `type` 而非系列）。数字由服务端写进 `data-dup`，页尾脚本读它重写文案，口径只存在于 `duplicateCountOf()` 一处。探针除读渲染出来的文案，还**只凭可见单元格独立重算**一遍重复数（两路都得 7 组 / 8 条才过），并跑两份变异体：属性与文案一起改坏 → 「初始文案」FAIL；只改属性 → 「静态文案同源于 data-dup」FAIL、而「筛选后文案」照属性读出 999（证明客户端真在读属性）。顺带修掉探针自身的第三个坑 —— **「兜底复跑」会让脚本跑两轮**，第二轮读到的是第一轮交互后重写过的文案，两份变异体因此收敛成同一份输出（§17.8） |
| 2026-09-13 | 修掉预览导出的**锚点漏改**（`scripts/export-preview.mjs`）：改写规则原先要求属性值在站内路径后**立刻结束**，于是 `/albums/<编号>/#tracks` 这种带锚点的链接整条漏改 —— 曲目库的曲名链接正是这个形状，离线预览里点它会跳到不存在的路径。改为把 `#锚点` 单独捕获后原样接回（`./GKCD-013.html#tracks`）。**这是条既有 bug**（上一版曲目库就在漏），一直没被发现是因为验收用的 grep 只查了 `src="/` 与 `/fonts`，没查 `href="/albums` —— 检查项本身的盲区，已补上 |
| 2026-09-13 | 重复计数括号改为**两处都带**（§17.10 改写）：页头摘要与表格上方计数都出「（含 8 首重复乐曲）」，括号**紧跟在曲数后面**（挂到「23 张专辑」后会被读成在修饰专辑）。文案真源收敛到 `lib/tracks.ts` 的 `dupNoteOf()` —— 服务端两处共用同一个变量，客户端脚本改读 `data-dup-note` 属性（原先它自己拼串，同一句话等于散落三处、改文案必漏）。元素上两个属性分工：`data-dup` 是数字（供校验/探针），`data-dup-note` 才是文案来源。探针新增「两处括号逐字相同」断言并重做变异体（A 只改属性 → 4 条 FAIL 且筛选后读出 999，证明脚本真读属性；B 只改页头括号 → 恰好击穿「两处一致」）。顺带修掉探针自身的两处错：断言正则把 `） · ` 写成 `）·`（**应从真实文案里抄，不能凭记忆写**）、变异体正则未锚定元素以致只改了 `<meta name="description">`、DOM 纹丝不动而输出与真实体逐字一致（§17.8 坑 5） |
| 2026-09-13 | 动态时间线的**日期改为视觉锚点**（`Timeline.astro`）：原先 13px / `--ink-300`（三级灰，同一套色阶里最浅的一档），扫列表时等于没有日期。现在 22px / `--ink-800` / 字重 500，并加 `letter-spacing: -0.02em`（大数字收紧才不散）。字号卡在 h3(18px) 与 display 之间偏下 —— 再大就会盖过区块标题；颜色用 `--ink-800` 而非 `--ink-900`，留一线给正文强调。配套：左列 52 → **64px**（窄屏 36 → 44px、字号降一档到 20px，否则两位数字贴列边），分组标题 `--ink-400` → `--ink-500`（日期变重后组标题会显得更轻，得跟着抬一档），与右侧 12px 的浅蓝标签形成「日期重、标签轻」。验证读**计算样式**（22px / `rgb(28,34,41)` / 500、文字实测宽 23.6px < 列宽 64px 即不折行、日期与标签顶差 2px、窄屏无横向溢出），并跑变异体把样式压回旧值 → 4 条判定全 FAIL。踩到的两个坑：① `.tl__day` 是 grid item 会被拉伸到**整行高**，量盒高永远测不出折行 —— 改用 `Range.selectNodeContents()` 量文本自身宽度；② 探针里 `'文案' + parseFloat(x) >= 500` 少了括号，`+` 抢先于 `>=` 变成字符串比较，恒为 false（**判定表达式必须自己先验一遍**） |
| 2026-09-13 | M3 · **动态 `/news`**（§18）：内容集合 `news`（一条动态 = `src/content/news/*.md`，字段只有 `date` / `tag` / `image?` / `draft`，正文即文件本身），标签枚举 `NEWS_TAGS` 与 `ALBUM_TYPES` 同一条原则放在 `lib/types.ts`；新增 `lib/news.ts`（`getNews()` / `tagsOf()` / `excerptOf()`）、`Timeline` 改为吃集合条目并渲染 Markdown 正文、`/news` 页（年月分组 + 标签芯片 + 空状态）、`npm run new:news` 脚手架（合法标签从 `NEWS_TAGS` 源码里读，不再抄一份）。首页动态区与样张页一并改用真实集合，`sampleNews` 删除。正文排版（`.prose`）从组件里提到 `global.css`，供动态 / 专辑简介 / 日后的杂谈共用。预览导出补上 `news.html`，并修掉「正文相对链接被吃半截」—— `../albums/<编号>/` 在映射前先归一成绝对路径。验证：无头探针实点芯片，断言几何（首条目贴回容器原点、末组底贴时间线底、筛掉的月份组不占位），并跑变异体（`.tl__item[hidden]{display:grid!important}`）确认探针会失败；正文链接的配色与下划线改读计算样式判定（截图上看不清，别靠眼睛） |
| 2026-09-13 | M3 · **杂谈 `/blog`**（§19）：内容集合 `posts`（一篇文章 = `src/content/posts/*.md`，字段 `title` / `date` / `summary?` / `tags` / `cover?` / `draft`，正文即文件本身），标签枚举 `POST_TAGS` 放 `lib/types.ts`；文件名**不带日期前缀**（文件名会进 URL，改期不该让链接失效 —— 与动态刻意不同）；新增 `lib/posts.ts`（`getPosts()` / `postHref()` / `neighboursOf()` / `summaryOf()`）、`/blog` 列表页与 `/blog/[slug]` 详情页（返回链接、导语、正文、上一篇 / 下一篇、`BlogPosting` 结构化数据）；摘要工具 `excerptOf` 从 `lib/news.ts` 搬到 `lib/text.ts`（动态与杂谈共用一份口径）；`PostCard` 改为平铺 props 且**无封面不出图位**（原先渲染「封面待补」占位框，一屏全是灰框）；「上一张 / 下一张」抽成 `Pager.astro` 供专辑页与文章页共用（断点照抄专辑页原有的 640px，抽组件不改老页面行为）；`.tag-list` 上移到 `global.css`；首页与样张页改用真实集合，`samplePosts` 删除；新增 `npm run new:post`（合法标签从源码读）；预览导出补 `blog.html` 并自动发现文章页。验证：列表页与详情页两套探针 + 变异体（无封面强留 220px 图位 → 正文宽 1120→**220**，判定如实 FAIL），期间抓到一条**假断言** —— 「正文左边界与页头标题对齐」在变异体下照样通过（无封面时图位那格本就是空的，正文会自动落进第一格），已换成宽度断言 |
| 2026-09-13 | 首页主视觉接入看板娘（§20）：新增 `npm run oc`（`scripts/build-oc.mjs`）把母版 `src/assets/oc.png`（4252×3071 / 14.6MB）压成 `public/images/hero/oc-{640,960,1440}.webp`（24/42/80KB），档位由 `lib/media.ts` 的 `HERO_WIDTHS` 统一；编码核心从 `lib/covers.mjs` 抽成 `lib/encode-webp.mjs` 供两处共用（抽取后 69 个封面产物 md5 全等）。版面：900px 以上两栏、纵向居中，以下单栏且画面排在按钮之后；画面刻意不裁剪，构图交给 CSS。原画自带 #EFEFEF 纸底，与纸白页面差一整个色阶，只柔化边缘仍是一块矩形 —— 改为「同色柔光垫底（`.hero__art::before`）+ 画面自身四边遮罩」，并把底色核对写进 `build-oc.mjs`（采样母版四角中位数与页面里的柔光色比对，不一致即退出码 1；跑过变异体验证）。期间踩到并记录：**柔光不能用 `box-shadow`** —— 阴影被裁在画面矩形框之外、与画面的遮罩错开，交界处冒出一条 `242 → 250` 的亮缝，缩放截图与肉眼都看不出来，靠逐像素扫描才抓到；柔光横向外扩 12% 会探出视口右边，`.hero` 加 `overflow-x: clip`（不能用 `hidden`，那会把纵向也变成 `auto`） |
| 2026-09-14 | M3 · **关于 `/about`**（§21）：把需求 §2.6 成员 / §2.7 社团简介 / §7 转载规约**合并为一页**（参照同人社团常见做法），联系方式单独留给 `/contact`；页脚「转载规约」改指 `/about#terms` —— 文案全站只有一份。新增两个内容集合：`pages`（`src/content/pages/{biography,terms}.md`，成篇长文进 Markdown，不写进模板）与 `members`（一人一个文件、只有 frontmatter，卡片靠字段排版）。`MemberCard` 的状态由 `left` / `guest` 两个布尔（合法组合 4 种、有意义 3 种）收敛为 `MEMBER_STATUS = ['正式','合作','退出']`，`Member` 接口随之删除（形状真源归 schema，同 `Post` 那次）；排序用 `order`（社团不维护加入日期），分组顺序 = 枚举声明顺序。**正式是默认状态**：卡片不挂标记、分组标题不画，只有合作 / 退出被标出来（同 §13.3 的思路）。页首给一排本页目录（合并页必须给跳转），锚点加 `scroll-margin-top: 80px` —— 页头粘顶 72px，不留会「点了有反应但标题被压在导航下」。NAV 去掉「成员」「社团简介」、新增「关于」；首页主视觉第二颗按钮改指 `/about/`；样张页改用真实成员，`sampleMembers` 删除；预览导出补 `about.html`。验证：1360 / 512 两档无头探针断言几何（目录三条锚点都有落点、三块 y 递增、每组网格首卡贴原点、正文宽 680、真实点击规约落点 y=80、横向溢出 0、页脚链接被改写成 `./about.html#terms`），三条变异体各自如实 FAIL（正文宽 680→1120、落点 80→0、首卡左 109→149）；其中「首卡贴原点」首轮误报 `false`，原因是 `Reveal` 未触发时 `.reveal` 上的 `translateY(14px)` 把卡片矩形推下 14px —— **量到的是入场动画，不是版位**，已在探针里显式关掉位移（与关掉平滑滚动同为测试台调整） |
| 2026-09-14 | 成员状态**改词**（§21.2）：`客座 · 合作` → `合作`、`离队` → `退出`。因为枚举值就是页面上的字（`MEMBER_GROUP_LABEL`），这次改的是 `MEMBER_STATUS` 本身 —— 分组、卡片标记、schema 校验三处一起跟着变，没有第二处要同步；三个成员文件的 `status` 一并改，`MemberCard` 的降调类名 `.is-left` 顺势改成 `.is-out`（旧名照的是枚举里的旧词）。验证：临时放一名 `status: 退出` 的成员，构建后核对分组标题与卡片标记两处都渲染「退出」、卡片带 `is-out` 类且该类在产物 CSS 里确实有规则，随后删掉临时成员重建 |
| 2026-09-14 | `/about` 增加「链接聚合」`#links`（成员与规约之间，§21.7）：放社媒账号与群组。新增内容集合 `links`（一个平台一个文件）、`lib/links.ts`、`LinkCard.astro`；分组枚举 `LINK_GROUPS`（社交平台 / 音乐与发布平台 / 群组）落在 `lib/types.ts`，取值即分组标题、顺序即声明顺序。卡片三种形态由数据决定：有 `href` 整卡是外链、只有 `handle` 给「复制」按钮（拿不到剪贴板时退化成选中号码）、都空显示「待补充」。**页脚「Elsewhere」改为读同一份数据**（只取有 href 的），`site.ts` 里 href 全空的 `SOCIAL` 删除 —— 一处维护，两处不再可能对不上。七个平台文件先建成骨架（值留空、注释写明怎么填）。踩到并修掉一个真坑：YAML 里不加引号的群号是**数字**，`z.string()` 会报「Expected type "string", received "number"」，schema 改为收 string 或 number 再转字符串。验证：1360 / 512 两档探针共 14 条断言，另放两条临时条目跑全两种卡片形态并用 mock 剪贴板跑通复制交互；三份变异体各自 FAIL，再把枚举声明顺序临时调换以证明分组顺序真来自声明 |
| 2026-09-15 | M3 · **联系 `/contact`**（§22）：需求 §2.8 一直是空的，照 §2.7 剩下两条（邮箱入口 / 是否接委托）立成两块的页 —— **联系方式**（入口卡）在前、**合作与委托**（说明）在后。新增内容集合 `contacts`（一条入口一个文件、只有 frontmatter）、`lib/contacts.ts`、单页文案 `content/pages/contact.md`。字段定义与 `links` **共用** `content.config.ts` 的 `entrySchema`（形状一样，为什么不合成一个集合见 §22.1）。邮箱刻意**只填 `handle`、不填 `href`**（§22.3）。复制逻辑抽成 `components/CopyScript.astro`（不能放 `LinkCard` 里：`is:inline` 用几次渲染几份；也不能改成普通 script：模块脚本在 `file://` 下被 CORS 拦掉，离线预览的复制会静默失灵）；`.grid-links` 从 `/about` 的 scoped 样式升到 `styles/global.css`（§22.4 / §22.5）。JSON-LD 用 `ContactPage`，`Organization` 补 `@id` 并与 /about 同源，`contactPoint` **只在真有邮箱时才写**。验证：干净态 15 + 回归 13 条、临时条目一轮 30 条全 PASS，五份变异体各自如实 FAIL；另踩两坑（探针注释里的标记字面量会被抢先匹配、`--window-size=1360` 实测 innerWidth 只有 1338） |
| 2026-09-15 | 联系方式加入 **X 与 Bilibili**（§22.6）：同一个平台的地址在 `/about#links` 与 `/contact` 各存一份、`note` 不同（不做跨集合的隐式引用）；X 的地址待填。顺带修掉 `npm run dev` **冷启动**的 `PARSE_ERROR`（§22.7）—— `.astro` 注释里的 HTML 标签字面量会绊倒 Vite 的依赖扫描 |
| 2026-09-15 | 新增 **站内搜索 `/search`**（§23）：索引由内容集合现场推导（`lib/search.ts`），**不引 Pagefind / Fuse.js**（索引本可推导、体量也用不着倒排索引，还省掉一个二进制依赖与一次请求）；分类枚举 `SEARCH_KINDS` 落在 `lib/types.ts`，**声明顺序即分组顺序**；164 条结果全部由服务端排进 HTML，客户端只做过与不过的判定 —— **没有 JS 时这一页就是一份完整的站内索引**；`data-search` 与曲目库同一套写法；`?q=` 可分享可刷新、细指针才自动聚焦；`pages` 三篇长文的去处写死在一处并做**构建期双向断言**；`Timeline.astro` 每条动态补 `id` 与 `scroll-margin-top: 88px`，搜索结果得以落到那一条；`.input` / `.toolbar*` / `.filters__search` 从 `/tracks` 的 scoped 样式上移到 `global.css`；预览导出补 `search.html`。验证：1360 / 512 两档各 39 条、`?q=` 一轮 36 条、减少动态效果一轮 30 条全 PASS，两份变异体各自如实 FAIL，另调换 `SEARCH_KINDS` 声明顺序以证明分组顺序真来自枚举 |
| 2026-09-15 | 新增 **首页进场遮罩**（§25）：版式照 logical-emotion.com / jizue.com（满屏纸色 + 极小的居中指示物 + 文档就绪后淡出 + 主内容错开浮入），但指示物换成**一条 1px 的弧从 12 点画满整圈** —— 跳点是通用 loading 的语汇，说的是「后台在忙」，而这个场景是「开场」，弧又正好是「月」的轮廓。`pathLength="1"` 归一化周长，样式里不写死 `2πr`。**「要不要播」的判断放在 head 的同步脚本里**（放 body 就已经晚了，会先露一帧首页再补上），三道门为「不是首页 / 减少动态效果 / 本会话已进过站」，靠 `html` 的 `data-intro` 与组件接头；不播时遮罩的默认样式就是完全不可见，禁用 JS 时也如此。两条兜底：`load` **且**已亮满 1200ms 才退（只等 `load` 会因缓存命中闪一下就没）、2600ms 强退加 head 里一道 8s 兜底（宁可没有动效也不能白屏）。退场时把弧钉在「已画完」（`in` 的动画被撤掉后 `stroke-dashoffset` 会回落到 1、弧会当场消失），`data-intro` 只改不删（主视觉浮入靠它驱动）。验证：三条门禁分支共 44 条断言全 PASS、四份变异体各自如实 FAIL；踩到「采样点写死会越过窗口」与「居中别拿带字距的文字当基准」两坑 |
| 2026-09-15 | 新增 **页面切换过渡**（§24）：出场（点站内链接 → 纸色幕布淡入 180ms → 跳转）在 `BaseLayout` 的页尾脚本，入场（内容淡入 320ms）是 `global.css` 里一条纯 CSS 动画；两半靠「幕布色 = `html` 底色 = `body` 底色」同一个 `--paper-50` 接上；**只用 opacity 不用位移**（位移会给 body 造包含块、带歪粘顶页头）；不接管外链 / 页内锚点 / 修饰键 / `target=_blank`；两个兜底 —— 跳转没走成 1.2s 后收幕、bfcache 回来时 `pageshow` 撤标记（否则后退会停在整页空白）。没有用 Astro 的 ClientRouter：它会让全站 `is:inline` 脚本不再重跑，得逐处补 `data-astro-rerun`，漏一处的症状是静默失效。§6 的「明确不做页面转场动画」按需求 §5 改口 |
| 2026-09-15 | 首页进场遮罩的**退场改为三拍交接**，并修掉一处**只在产物里发作的时长 bug**（§25.4）：① 指示物（弧放大着散开、两行字上移着淡出）先走，不再跟着纸面一起淡 —— 从前「弧 / 站名 / 纸面底下的大标题」会在同一处各带半透明叠着，那一段就是「接不上」；② 纸面延后 240ms 才揭，并**改用自己的一条缓入缓出**（`--intro-veil-ease`）—— `--ease` 是五段缓出，拿它揭一整块纸时，名义 460ms 的动作在头 60ms 里就走掉一半（探针逐刻量出来是 0.5 / 0.16 / 0.05），读起来仍然是「闪一下」；③ 主视觉提前到纸面还全遮时（120ms）就开始上浮，于是「揭开」看到的是已经就位的画，最后一件也在纸面揭完前落位（780ms 对 700ms）。退场从 transition 改成 animation，图的是能被 `pause()` + `currentTime` 定格逐帧对账。**顺带修掉两个真 bug**：`Intro.astro` 与 `BaseLayout.astro` 原先都用 `parseFloat` 读时间令牌，而压缩器把 `200ms` / `180ms` 写成 `.2s` / `.18s` —— 于是产物里最短显示时长是 **1.2ms**（弧刚起笔就退场）、摘节点时限 **60.7ms**（纸面淡到一半被整个抽掉）、出场幕布 **0.18ms**（刚点亮就跳走，那半场等于没跑）。「弧画完 + 停一下」这个下限 DESIGN 里一直这么写着、代码里却是写死的 700ms —— 文档与代码漂移了，这次让代码追上文档（`msOf()` 按单位换算）。验证：五轮共 83 条断言全 PASS，九份变异体各自如实 FAIL，其中「`msOf()` 换回 `parseFloat`」一份如实复现出 `[1.2, 20, 60.7, ...]` |
| 2026-09-15 | 站点简介**改写**（`site.ts` 的 `SITE.description.zh`）：旧句「月華社是一个同人音乐社团，创作原创音乐与东方 Project 二次创作编曲。」只提「原创 + 东方」，既漏掉社团已有的蔚蓝档案主题合作盘，也与 `/about` 正文里「除原创外，目前在东方Project、蔚蓝档案等IP也有涉及」对不上；新句「月華社是一个同人音乐社团，目前涉足有原创、东方Project、蔚蓝档案等IP的同人音乐创作。」把 IP 写成开放清单。**一处三用**（首页主视觉导语 / meta 与 og 描述 / `/about` 的 `Organization` JSON-LD）—— 改这一处三处同时更新，构建后核对 `dist/index.html` 2 处、`dist/about/index.html` 1 处，旧句全站零残留；离线预览重导出，免得对照停在旧文案。ja / en 两条是搁置的暂译、无页面引用，未同步改 |
